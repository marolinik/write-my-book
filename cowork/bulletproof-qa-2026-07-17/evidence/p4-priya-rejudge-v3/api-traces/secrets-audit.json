{
  "filesScanned": 27,
  "secretsClean": true,
  "literalSecretHits_raw": { "E2E_TEST_SECRET": 1 },
  "hit_analysis": "The single hit is a FALSE POSITIVE. The dev E2E_TEST_SECRET value is a low-entropy placeholder ('test-secret'-class, 11 chars) that is a SUBSTRING of the HTTP header-NAME string literal \"x-e2e-test-secret\" in scripts/_helper.ts. That file never contains the secret VALUE as a credential — it references process.env.E2E_TEST_SECRET and passes it only through the `SECRET` variable (`\"x-e2e-test-secret\": SECRET as string`). No api-trace, poll-timeline (.jsonl), screenshot, or doc contains the secret as an actual header value or credential.",
  "secretPrefixHits": {},
  "byok_key_leaked": false,
  "database_url_leaked": false,
  "conclusion": "No credential is leaked anywhere in the bundle. secretsClean = true. The only real-secret handling is process.env-based; BYOK/Priya key never decrypted into any artifact."
}
