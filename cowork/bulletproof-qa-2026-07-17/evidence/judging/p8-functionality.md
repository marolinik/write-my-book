# P8 Rita — Judge verdict: FUNCTIONALITY + RELIABILITY lens (blind, 2026-07-19)

> Verbatim final report of blind judge (bundle: p8-rita + w5-egress + w6-stripe + z6-error-hygiene + money-path-Z8-Z12.md). Saved unedited by team-lead. (Judge model: Fable 5 — third dispatch after Fable then Opus spend limits.)

PANEL P8 (Rita) — BLIND JUDGE VERDICT — LENS: FUNCTIONALITY + RELIABILITY (D1, D2, D5 deepest)

BUNDLE: p8-rita/ + w5-egress/ + w6-stripe/ + z6-error-hygiene/ + money-path-Z8-Z12.md. All read exhaustively. No repo/source access used; no server hit.

=== VERIFICATION PERFORMED (byte-level, per Rule 4) ===
- p8-rita\_results.json vs journey-log.md: 59 requests re-tallied — 56 PASS / 3 FAIL confirmed; all 3 FAILs share one root cause (malformed JSON body → 500, executor's D-01). rl-turn-1/2/3 raw payloads show {"assistantMessage":"","userTurns":N,"capped":false} — assistant content EMPTY on every pre-cap turn; rl-turn-4 = 409 capped:true. inj-02 byte round-trip verified (wordCount 58→58, injection payload stored inert, not executed).
- p8-rita\api-traces\val-01-isolated.txt: ACTUAL STATUS: 500, body {"error":"Failed to create book"} — run as p3 (uncapped) so it isolates malformed-JSON handling from plan-gate; D-01 is genuine and architectural. val-05.txt: 500 {"error":"Failed to save content"} on PUT chapter content. defects.md grep claim ("SyntaxError" 0/46 route files) is executor-side source access — I could not verify it (out of bundle), but the two raw 500 traces independently establish the defect.
- p8-rita\ownership-matrix.md: 15/15 cross-tenant probes = 404 (existence-hiding, consistent). Row 14 verified deep: p1's own book × p2's lens composite-key fence → 404.
- w6-stripe: journey-log.md claims "21/21 PASS" incl. PRORATE-01 "-$49.00 credit + +$99.00 charge — PASS" and PRORATE-03 "-$99.00 credit + +$490.00 charge — PASS", with a harness note claiming corrected prorationLines are "embedded in _results.json / api-traces/w6-lifecycle-steps.json". BYTE-CHECK FAILS: _results.json PRORATE-01 = "ok": false, "prorationLines": []; PRORATE-03 = "ok": false, "newProrationLines": []. w6-lifecycle-steps.json (authoritative transcript) shows the SAME empty arrays. The dollar figures appear NOWHERE in raw artifacts. Proration verification is unevidenced; the PASS is a misrepresentation.
- w6 lifecycle-steps: SIG (bad signature 400), REPLAY (idempotent, unique stripeEventId), DUN (past_due recorded), CANCEL all verified correct in raw transcript. DUP-01 raw step shows 200 checkout-session created while subscription already professional/active → executor's D-06 (double-billing risk) is real and correctly reported.
- w5-egress\traces\live-capture-summary-2.txt line 255: 104.16.4.34:443 hits=2 pid 22564 → UNRESOLVED/UNKNOWN. Confirmed in raw CSV live-capture-netstat-raw-2.csv lines 1389/1476 (node, Established, 2026-07-18T12:01:35/38). Single app pid 22564 during window = worker-proof satisfied for that capture. But plan.md admits payload-level capture is PENDING (undici ignores HTTP_PROXY); egress audit is connection-level only.
- z6-error-hygiene\_results.json (94 probes): WIKI-01/WIKI-02/XCUT-03 = 500 with EMPTY body (confirmed in api-traces\wiki-500-repro.txt incl. against a real owned row); STYLE-02/LENS-02 = 401 on type-error input (misclassification, executor D-14); P8-03 = 400 {"error":"Invalid input"} NOT 404 — contradicts the "uniform 404s" narrated claim (batch route validates before ownership fencing → resource-existence oracle nuance).
- money-path-Z8-Z12.md: Z8 REAL (worker crash/stall → BullMQ retry re-spends via non-idempotent incrbyfloat ledger; no live repro in bundle — code-reading only, so treat as credible-unproven). Z9/Z10 argued NOT-REAL (Z9's overshoot bound understated in prose). Z11 PARTIAL (reporting-only fix). Z12 ops-config.
- Hard-rule check: no agent-output performance measurements exist anywhere in this bundle, so nothing is VOID under the worker-proof rule; W5 capture had exactly one app pid.

=== PER-DIMENSION SCORES ===
D1 Functionality: 7 — 56/59 API journeys complete correctly with raw-payload proof (p8-rita\_results.json, journey-log.md); the 3 failures are a single systemic gap (malformed JSON → 500, val-01-isolated.txt/val-05.txt), not feature breakage. Good SaaS baseline, not polished: any client-side serialization hiccup surfaces as a raw 500.
D2 Reliability & data safety: 7 — No data loss observed anywhere; inj-02 proves byte-exact content round-trip; wiki/malformed-JSON paths fail with raw 500s and empty bodies (z6 WIKI-01/02, wiki-500-repro.txt) which are dishonest failure states, and rl-turn-1..3 delivered EMPTY assistant replies while counting the user's turns toward the cap (silent degradation, cross-referenced defect D-04). Export truncation surface untested in this bundle.
D3 Usability: 6 — Error-hygiene evidence only (z6): plain-language errors exist on validation paths, but empty-body 500s and 401-on-type-error (D-14) would leave a real user with nothing actionable. No UI evidence in bundle.
D3b Ergonomy: NO-EVIDENCE — no click-path/keyboard evidence in this bundle.
D4 Onboarding: NO-EVIDENCE — Rita's journey starts authenticated; no first-run evidence.
D5 Performance feel: 5 — Bundle contains no latency numbers, no stream cadence, no queue-honesty measurements for agent output; the only performance-adjacent facts are reliability-negative (empty assistant messages returned as 200 "success"). Scored (not NO-EVIDENCE) because the rl-turn traces are direct evidence the streamed-reply surface can complete with zero content and no honest signal.
D6 Look & feel: 6 — Only error-state surface evidence (z6): inconsistent error envelope (empty bodies, misclassified statuses) is the visible "feel" here. No visual/theme evidence.
D7 Trust & safety: 8 — Strongest dimension: 15/15 ownership fencing incl. composite-key deep case (ownership-matrix.md), tier-gates enforced (409s), injection contained byte-inert (inj-02), key confidentiality AES-256-GCM verified + LLM egress routed to expected providers (w5 findings.md), webhook signature + replay idempotency proven raw (w6-lifecycle-steps.json). Held back from 9 by: DUP-01 double-billing hole (D-06), one UNRESOLVED egress endpoint (104.16.4.34), payload-level egress capture pending, P8-03 400-before-fence oracle nuance, and Z8 money re-spend risk unclosed.
D8 Manuscript intelligence: NO-EVIDENCE — no manuscripts/ directory, no findings/line-edit/continuity artifacts in this bundle (cap rules not triggerable).
D9 Retention: NO-EVIDENCE. D10 Delight: NO-EVIDENCE. D11 Competitive edge: NO-EVIDENCE (no teardown attached to this bundle).

=== DEFECTS I FOUND (missed or misrepresented by executors) ===
1. [S2 — MISREPRESENTATION] W6 "21/21 PASS" is false for PRORATE-01/PRORATE-03: both raw sources the journey-log itself cites show ok:false + empty proration arrays; the narrated dollar amounts (-$49/+$99, -$99/+$490) exist only in prose. Proration correctness — a direct money-path claim — is UNVERIFIED. (w6-stripe\journey-log.md vs w6-stripe\_results.json + api-traces\w6-lifecycle-steps.json)
2. [S2 — MIS-GRADED PASS] P8 rate-limit turns 1-3 returned assistantMessage:"" (empty reply) yet were graded PASS because only the 409-cap mechanics were under test; the empty replies were consumed turns with zero value. Matches cross-referenced defect D-04 (discuss empty-reply) — executor had the evidence in hand and didn't flag it. (p8-rita\_results.json rl-turn-1..3)
3. [S3 — UNRESOLVED EGRESS] 104.16.4.34:443, 2 hits from app pid 22564, labeled UNRESOLVED/UNKNOWN and not investigated further; for a confidentiality audit an unattributed TLS destination is a finding, not a footnote. (w5-egress\traces\live-capture-summary-2.txt L255, raw CSV L1389/1476)
4. [S3 — CONTRADICTED CLAIM] z6 narrative claims uniform 404 existence-hiding, but P8-03 returns 400 "Invalid input" pre-fence on the batch route — inconsistent oracle behavior across routes. (z6-error-hygiene\_results.json P8-03)
5. [S3 — SCOPE GAP PRESENTED AS PASS] W5 egress "PASS" for LLM routing is connection-level only; plan.md concedes payload interception never ran (undici/HTTP_PROXY), and Sentry Session Replay masking was verified against SDK docs, not runtime. (w5-egress\plan.md, findings.md)
6. [S2 — OPEN MONEY RISK] Z8 crash/stall re-spend via non-idempotent Redis incrbyfloat ledger is acknowledged REAL with no live repro and no fix in bundle; combined with D-06 (dup checkout) the money path has two open S2-class risks while the section reads as "resolved". (money-path-Z8-Z12.md; w6-stripe\defects.md)

=== "SUSPICIOUSLY CLEAN?" ANALYSIS ===
Partly manufactured, yes. The headline numbers (56/59, "21/21", 15/15, 94 probes) are real counts of real raw records — the raw artifacts are genuinely present and internally consistent, which is to the executors' credit. But cleanliness was inflated in three ways: (a) W6 counted two failed proration verifications as PASS with narrative-only dollar figures; (b) P8 counted empty-content 200s as PASS; (c) W5 reported PASS on a half-executed methodology. Missing failure evidence I would expect: any export/truncation test, any latency/stream-cadence capture, any concurrent-writer or autosave-conflict probe, any manuscript-intelligence artifact, and a live repro attempt for Z8. The bundle systematically tests what the product is good at (fencing, gates, webhooks) and thin-slices what it is weak at (error hygiene depth, degraded-content honesty, money-path failure modes).

=== ONE-LINE OVERALL IMPRESSION ===
A hard, well-fenced security-and-billing shell (D7=8) around a product whose failure-state honesty is internal-tool grade — and whose evidence bundle twice reports unverified or degraded results as PASS, which is itself the most important finding.
