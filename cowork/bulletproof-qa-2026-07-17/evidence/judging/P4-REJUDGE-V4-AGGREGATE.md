# P4 re-judge v4 — AGGREGATE (3-judge blind Fable panel, 2026-07-27)

## Verdict: **6.0 UNANIMOUS — UNCHANGED from v3.** D-120/D-122 closures endorsed 3/3 (source+test, shared-module shape praised) but scored as unwitnessed: no live batch ran this cycle.

| Dim | A | B | C | Median (v3) |
|---|---|---|---|---|
| D1 | 7.0 | 7.0 | 7.0 | **7.0** (7.0) |
| D2 | 6.5 | 6.5 | 7.0 | **6.5** (6.5) |
| D3 | 6.5 | 6.5 | 6.5 | **6.5** (6.5) |
| D3b | 6.0 | 6.0 | 6.0 | **6.0** (6.0) |
| D4 | NO-EV | NO-EV | NO-EV | — |
| D5 | 6.5 | 6.5 | 6.5 | **6.5** (6.5) |
| D6 | 6.5 | 6.5 | 6.5 | **6.5** (6.5) |
| D7 | 7.0 | 7.0 | 7.0 | **7.0** (7.0) |
| D8 | 6.0 | 6.0 | 6.5 | **6.0 FLOOR** (6.0) |
| D9 | 6.0 | 6.0 | 6.0 | **6.0** (6.0) |
| D10 | 6.5 | 6.0 | 6.0 | **6.0** (6.0) |
| D11 | 6.5 | 6.5 | 6.5 | **6.5** (6.5) |

## Floor: D8/D3b/D9/D10 @6.0 — a LIVE-PROBE plateau, not a code gap
Common driver (3/3): the unprobed silent-zero/intelligence cluster — **D-121** Apply never clicked, **D-123 S3** halted-digest zero undiagnosed, **D-126** rerun near-zero (dedupe hypothesis now cross-persona corroborated via D-159/D-107 AND structurally outside the new `suppressed` counter — dedupe suppressions early-return before create). **D-125** keeps the halt path UI-unreachable — judge C nuance: `min={1}` is advisory; submit gate is only `cap<=0||cap>25`, so a typed 0.005 likely submits = misleading affordance, one probe settles. Judge C echo of the campaign law: "code-only closure moves trust dims but cannot move experience dims."

## NEW registrations (next free was D-186)
- **D-186 (S4 family):** batch LIST/cancel residual staleness — (a) LIST serves stale completedCount/failedCount mid-run (columns only written by fan-in digest; judge B); (b) **cancel-mid-run resurrects the $0.00 spend lie**: cancel route flips status without writing spentUsd, terminal-verbatim rule then serves the stored row on BOTH routes until fan-in (judge A — D-96 lie class re-opened on the never-drilled path); (c) terminal-set divergence: cancel route's own TERMINAL_STATUSES (no "halted") vs shared TERMINAL_BATCH_STATUSES (drift-bait). One family, one fix lane.
- **D-187 (S4, judge C):** no batch-history surface — the honest LIST endpoint has ZERO UI consumers (only fetch is the POST); the D-120 truth is unreachable by any writer. Bounds D3/D9/D11.
- S5 notes (unnumbered): all-failed pre-digest children derive "queued with spend"; long-child under-report window; "(N discarded)" false-comfort (dedupe zeros still unexplained).
- **Next free: D-188.**

## Next-action consensus (3/3 identical): ONE instrumented live batch drill on the fixed build
Land the D-125 rider first (min/step 0.01 + honest submit gate) so the halt path is drivable on camera, then in one session: (1) render the "N findings (M discarded as invalid)" morning notification in pixels; (2) sub-cent halt + dump EditFinding by halt child ids vs digest totals (D-123); (3) byte-identical rerun near-zero check (D-126); (4) Apply click on a hard-wrapped-prose anchor (D-121); (5) mid-run LIST poll + mid-run cancel poll (D-186 a+b live). Attacks all four floor dims; nothing else comes close.

Raw verdicts: `p4-v4-raw-verdicts.md`. Bundle: `p4-v4-judge-bundle.md`. Prior: P4-REJUDGE-V3-AGGREGATE.md (6.0).
