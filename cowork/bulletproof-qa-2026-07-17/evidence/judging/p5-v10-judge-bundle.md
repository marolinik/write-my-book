# P5 re-judge v10 — delta bundle

**Date:** 2026-07-26 · **Build:** `c253830` · **Delta on:** v9 aggregate (`P5-REJUDGE-V9-AGGREGATE.md`, P5 = 6.0, floors D1 + D11 @6.0)

## New since v9 — both floors attacked

### D1 (floor) — cold funnel captured end-to-end (§v9 addendum, shots 39-series, ALL UNSTAGED)
Fresh identity through: honest empty shelf → New Book form (write-first primary vs guided) → create lands DIRECTLY in the chapter editor → first sentence typed → first ghost pause answers the honest **top-anchored no-key toast** ("No API key configured for ghost text. Add one in Settings > API Keys.", data-y-position=top). Two taps + typing = signup-to-first-word. Found on the way: **D-153** dev-harness crash (2nd dev identity P2002 on hardcoded email — FIXED `4d2c0dd`, dev-only path) and **D-154** silent empty-submit.

### D11 (floor) — D-137 immersive silent-wall FIXED (`c253830`, lane wf_cd284587-69c)
Shape (b): ghost quick-assist fully suppressed while immersive (`active = enabled && !immersive` through every gate; entry aborts in-flight unbilled per D-142). The billable-invisible pairing is dead at the ROOT — no fetch, no bill, no invisible 429. Deliberate immersive chrome suppression preserved. 3 tests incl. mid-flight-abort-on-entry. Outside-immersive banner path byte-identical (audited).

### D-154 also CLOSED on camera (shot 40): role=alert + aria-invalid + focus, localized ×7.

## Instructions
Same dimension set + precedents. Verify: shots 39a/39b/39c/39g/39h/40 with Read; code delta `1de4ef0..c253830` (auth.ts D-153; ai-ghost-text.tsx/manuscript-editor.tsx D-137; books/new + ui-strings D-154). Suite 1419/1419, tsc 0. Registered-open, do not re-discover: D-140/141/146/147/148/149/150/151/152. Still-open disclosed gaps: inline-edit-on-touch camera, prod latency (D-128), D-149 re-engagement channel (founder-call). Score all ten dimensions; name the new floor and its cheapest honest lift.
