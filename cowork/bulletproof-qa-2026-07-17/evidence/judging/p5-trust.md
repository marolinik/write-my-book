# P5 Sam — Judge verdict: TRUST + MANUSCRIPT INTELLIGENCE lens (disqualifier hunter) (blind, 2026-07-18)

> Verbatim final report of blind judge (bundle: p5-sam + w4-ui-drills + z6-error-hygiene + competitive-teardown.md). Saved unedited by team-lead. (Judge model: Opus 4.8 — re-dispatched after Fable spend limit.)

I have now exhaustively reviewed every file in the bundle and byte-verified the load-bearing claims against raw payloads and screenshots. Final verdict follows.

---

# PANEL P5 (Sam) — BLIND JUDGE VERDICT
**Lens: TRUST + MANUSCRIPT INTELLIGENCE (disqualifier hunter)**
**Bundle:** p5-sam/ + w4-ui-drills/ + z6-error-hygiene/ + competitive-teardown.md

## Byte-verification performed (not trusting executor summaries)
- **Paywall copy** — `screenshots/books-new-upgrade-modal_390x844_light.png` renders exactly "Upgrade Required / Your subscription is inactive. Subscribe to access this feature. / The Indie Author plan gives you 2 active books with all 14 AI agents and workflows. / [Cancel] [View Plans]". Matches D-08 quote AND the raw API body in `api-traces/00-create-book.json` (`{"error":"Your subscription is inactive...","upgradeToTier":"indie"}`). Form field "Dragon Rider Fanfic QA P5" is visibly preserved above the modal → traps-words=false CONFIRMED.
- **D-12 Arabic fallback** — CONFIRMED. `settings_..._locale2-ar.png` is byte-identical English to `...-en.png` (only the dropdown reads "Arabic"), while `-fr.png`/`-zh.png`/`-de.png` fully localize ("Paramètres"/"设置"/"Einstellungen"). No RTL applied (layout stays LTR in ar). German (the one locale the prose never explicitly states) verified clean, so the "5 of 7 clean" claim holds.
- **D-11 nav-never-translates** — CONFIRMED. Bottom nav "Home/Books/Agent/Settings" stays English in every localized screenshot (fr/zh/de).
- **D-23 / D-24 / Z13** — CONFIRMED against raw failure logs. `d23-...txt`: expected `clean-exit-1784342590950`, received `<p>950Z14 baseline...</p>` (surviving chars prepended at pos 0). `d24-...txt`: expected `crash-marker-...`, received baseline only (100% loss). `z13-...txt`: genuine Radix `useId()` hydration mismatch (server `_R_6bmulbH1_` vs client `_R_qbmulbH1_`).
- **Z6 leak sweep** — spot-verified `_results.json`: WIKI-01/02/XCUT-03 = 500 empty body (D-15), STYLE-02/LENS-02 = 401 on wrong-type (D-14), all cross-tenant = 404 no-leak, XCUT-04 "leak" is a Turbopack public chunk path (true false-positive).
- **Two-tab conflict** — `x1-b-conflict-dialog-open-diff.png` shows honest "Chapter changed outside this editor" dialog + diff + Cancel/Load theirs/Keep mine + non-blocking "Review" chip; loser's words stay live. Strong data-safety UX.

## Per-dimension scores (re-derived, executor grades ignored)

**D1 Functionality — 6.0.** Reachable surfaces work (settings, BYOK key mgmt, language, dashboard, conflict machinery). But Sam's *core* journey (write a word) is 100% walled, and the bundle contains real functional defects: D-23 immersive corruption + D-24 crash-loss (`w4/console-captures`), D-15 wiki raw-500 (`z6/api-traces/wiki-500-repro.txt`), and teardown-cited broken chapter reorder. Much of D1's core is unexercised for this persona.

**D2 Reliability & data safety — 6.0.** Split verdict. Best-in-class on conflict/network: two-tab 409 backs up the loser's words to a `conflict-backup` version before overwrite; network-kill shows "Sync pending"→self-heals to "Saved" (`w4/journey-log.md`). But TWO confirmed intermittent *word-loss* bugs, byte-verified in raw traces: D-23 (silent in-session loss on clean Escape, ~1-in-3, chars corruptly prepended) and D-24 (hard-crash TOTAL loss, no partial floor, ~50%). Both are S1-class "writer's words lost." The RE-VERIFY "FIXED-VERIFIED / 0-3 failures" is self-reported by the graded party against commits I cannot inspect — I treat the defects as proven, the fixes as claimed-unverifiable.

**D3 Usability — 7.5.** Plain-language, non-trapping paywall with Cancel/View Plans (no dead-end); honest empty states; conflict dialog gives real control (Cancel/Load theirs/Keep mine); network failure states are legible. Docked for D-15 (empty-body 500 gives the user zero to act on) and D-14 (misleading "Unauthorized" on a malformed field).

**D3b Ergonomy & efficiency — 6.5.** Zero horizontal scroll across 34 screen×viewport×theme combos down to 320px (`layout-findings.json`, every `horizontalScroll:false`), landscape reflows, all 15 tab-stops focus-visible, no dead-ends. But 8 icon buttons on the BYOK-config page have no accessible name — keyboard/switch users can't tell what they do on the one page whose job is key setup.

**D4 Onboarding / time-to-first-word — 3.5.** The defining failure for this persona. D-08: there is NO card-free path to a first word — even trials require a card (`checkout/route.ts:128-137`, `missing_payment_method:"cancel"`, cited in defects.md). All 4 dashboard doors (Create Book/Series/Start Writing/Import) funnel to the same wall. "Offers never wall" is violated immediately. The only mitigant is that the wall copy is honest and plain.

**D5 Performance feel — NO-EVIDENCE.** No agent/stream latency, no queue-honesty numbers, no stream cadence (Sam can't reach agents). Only infra health latencies (postgres 51ms/redis 77ms/schema 174ms) and a dev-server Turbopack compile timeout — none of which are writing-product performance feel.

**D6 Look & feel — 6.5.** Clean light+dark responsive across all screens, honest empty/loading/error states, reduced-motion renders without breakage. Docked hard on locale completeness: Arabic totally broken (D-12), no RTL anywhere, mobile nav never localizes (D-11), plus systemic duplicate-`<main>` and unfiled heading-order/missing-h1 a11y issues.

**D7 Trust & safety — 7.0.** Strong core: ownership-fence-before-plan-gate verified (all Sam AI surfaces 404; all cross-tenant p2/p8 probes 404 with no existence leak); **0 confirmed leaks across 94 probes** with the sole scanner hit correctly ruled a Turbopack false-positive; BYOK key masked (`sk-or-v...705e`); path traversal blocked; honest paywall/billing states. Docked for: D-15 unhandled exception (raw 500), D-14 wrong 4xx class, **injection containment untested in this bundle**, and the self-disclosed-but-unproven model-identity/billing dishonesty (see defects below) that — if real — is S1-class.

**D8 Manuscript intelligence quality — NO-EVIDENCE.** As the disqualifier-hunter for D8, I looked hard and found ZERO primary artifacts: no findings, no verbatim anchors, no line-edit before/after, no continuity flags, no discuss threads, no memory-constraint tests anywhere in this bundle. Sam (unsubscribed, zero-book) structurally cannot reach any editorial surface, and z6/w4 tested envelopes/data-safety, not intelligence. The competitive-teardown *describes* the quote-anchored/voice-fingerprint/WriterMemory loop but supplies no sample. The D8 misquote/false-positive caps are non-triggerable here because there is nothing to verify. Cannot score.

**D9 Retention / habit — 6.0.** Honest health surface: dashboard shows genuine 0/0/0 (Total Books/Words/Chapters) with no fabricated streaks or engagement (`dashboard_390x844_light.png`). But the actual streak/heatmap habit mechanics are not exercisable for a zero-history persona — evidence is limited to "does not lie about zero," which it passes.

**D10 Delight — 5.0.** For a walled hobbyist the experience is the opposite of delight — he hits a paywall from every door. The single genuine bright spot is the two-tab conflict backup UX (non-blocking chip + clear diff + words preserved), but that's really D2/D3 substance.

**D11 Competitive edge — 4.0.** For THIS persona, weak. The teardown's own honest conclusion: moats (series continuity, editorial loop) keep a "serious, technical, series-writing pro," but "a debut author or hobbyist bounces in the first hour." Sam's incumbents all beat wmb-pub on the on-ramp: Sudowrite (10k free credits, no card), Novelcrafter (21-day trial, no card, + Ollama), ChatGPT/Claude (flat $20, no meter). wmb-pub offers Sam no card-free path and no proprietary quality draw (no Muse-equivalent), and the self-disclosed model-identity dishonesty actively undercuts its transparency moat. Sam would not switch and pay.

## DEFECTS I FOUND that the executor missed or misrepresented
1. **Unfiled a11y: `books-new-form` has NO `<h1>`** — `axe-results.json` fires `page-has-heading-one (moderate)` on books-new-form, and `heading-order (moderate)` on books-list-empty and settings. D-09 only files the 4 landmark rules; these 3 heading violations are in the raw data but never filed as defects. Severity S3/S4. Transparent but under-reported.
2. **D-10 overstates the AT-blocking count** — defects.md claims "10 of 15 tab-stops are bare with no id/label" and "3 carry a label," but `keyboard-tab-order-settings.json` actually shows **12 bare** elements (8 `button` + 4 `a`) and only **2** with `aria-label` (+1 radix id). The 4 bare anchors did NOT trigger axe `link-name`, so they likely have text names — meaning the true AT-blocking count is the **8 buttons** (which axe confirms), not "10." Internal arithmetic inconsistency; core finding still valid.
3. **Batch route validates BEFORE ownership** — `z6 _results.json` P8-03: p8 (foreign, unsubscribed) POSTing to p2's book got `400 "Invalid input"`, and the executor's own note said "expect 404 ownership before any gate." The 400-instead-of-404 contradicts the campaign's stated ownership-fence-first invariant for the batch route specifically, and the executor did NOT flag the discrepancy. Not a leak (reveals nothing about book existence), but an unflagged ordering inconsistency. Severity S4.
4. **Model-identity / billing dishonesty asserted but not filed or substantiated** — competitive-teardown.md (§capability baseline, §7, §Synthesis note) alleges as a "live defect": Coach/conductor force-run on `${provider}/sonnet` regardless of the user's chosen model, discuss threads hardcoded to Claude Haiku, AuthorshipTracker faking "100% human," Story Radar/Daily Plan heuristic placeholders labeled "AI monitoring." **If true, secretly billing Claude Sonnet prices under a qwen label is an S1-class billing/trust violation** — yet it appears in NO defects.md in this bundle as a numbered finding and is backed by NO cost trace or run artifact. This is the single biggest under-substantiated trust claim in my lens; a downstream panel with agent/cost evidence must run this to ground.
5. **D-12 severity is arguable** — the teardown states only "7 of 14 UI languages [are] full," so Arabic-incomplete may be a *known-incomplete* locale rather than a false-success bug. The S2 "API lies about success" framing is defensible but could be inflated versus a plain "incomplete-locale S3."

## "Suspiciously clean" analysis
This bundle is NOT a whitewash — it is one of the more honest artifacts I could ask for: it surfaces D-08→D-15, D-23, D-24, Z13; flags NOT-TESTABLE honestly (expired-subscriber probe); self-corrects 4 harness bugs before filing; and labels dev-bypass evidence `*_DEVBYPASS-NOT-SAM_*`. The clean headline results (0 horizontal-scroll/34 screens, 0 leaks/94 probes) are each backed by raw per-item traces, so they read as earned, not asserted.

Where I remain skeptical: (a) the **RE-VERIFY "FIXED-VERIFIED"** section flips two HIGH word-loss defects to green within the same bundle, self-reported against commits I cannot inspect — exactly the graded-party self-grading the rubric declares non-binding; I score the defects as real and the fixes as unproven. (b) **The absence I most expected and did not get is any manuscript-intelligence failure evidence** — no findings, no line-edits, no continuity, no injection probe, no cost/model-identity trace. That absence is structurally explained by Sam's walled persona, but it means this bundle cannot corroborate a single one of the product's headline editorial/voice/transparency moats, and it leaves the teardown's own S1-grade billing-dishonesty allegation completely unverified.

## One-line overall impression
An unusually honest, evidence-dense sweep that proves excellent tenant-isolation and conflict-safety while exposing real word-loss, i18n, and a11y defects and a fatal hobbyist on-ramp wall — but for MY lens it is a near-total blank on manuscript intelligence (NO-EVIDENCE, D8) and leaves the most serious trust allegation (secret Sonnet-priced billing under a qwen label) asserted-but-unproven.
