# P6 Owen — Judge verdict: TRUST + MANUSCRIPT INTELLIGENCE lens (disqualifier hunter) (blind, 2026-07-18)

> Verbatim final report of blind judge (bundle: p6-owen + competitive-teardown). Saved unedited by team-lead. (Judge model: Opus 4.8 — re-dispatched after Fable spend limit. Harness note: safety classifier claude-sonnet-5 was unavailable during this subagent's run.)

I have byte-verified every finding, the memory causal chain, the outage fake-success, the trust surface, and the single-worker proof. I have everything needed to render the verdict.

---

# PANEL P6 (Owen — literary stylist) — BLIND VERDICT
Lens: TRUST + MANUSCRIPT INTELLIGENCE (disqualifier hunter). Every score re-derived from raw artifacts; executor grades treated as non-binding.

## Gate checks (done first, independently)
- **Worker-proof: VALID.** `worker-proof.txt` shows exactly one worker chain (PIDs 12744→43144→26536→48788, one `src/worker.ts` runtime), captured before the first agent run AND after the final pass (18:22) with identical PIDs, no restarts. Agent-output measurements are NOT void.
- **Misquote cap (D8 ≤8.5): NOT triggered.** I byte-checked all 7 findings' `originalText`/`anchorQuote`/alternatives against the manuscripts — not the executor's `misquote-probe-all.json` summary (which merely asserts `verbatim:true`). Qwen findings `bdb80574`/`9b7fbf43`/`ce63cb76`/`594ea427` all match `owen-ch5-what-the-water-keeps.md` verbatim (paras 6/6/7/7). Opus findings `4591a969`/`7a68493d` match ch5 verbatim; `0002c9e1` ("One held a clipboard with a laminated card clipped to the top of it.") matches `corpus-src-saltletters-ch3.md` line 33 verbatim. The opus critical `7a68493d` originalText spans two sentences and correctly stops before the colon, so it does NOT touch V1 and-stack. **0/7 misquotes, independently confirmed.** Cross-check: `strong-setup.json` chapter hashes prove the in-app chapters equal the pre-registered manuscript files (ch1 `983258a9…`=corpus-ch3, ch3/ch4 match), so my byte checks are against real content.
- **Continuity false-positive cap: N/A** — this is a line-edit journey; no continuity flags exist in the bundle.
- **Voice-flatten cap (D8 ≤8.0): NOT triggered.** Every finding targets a *planted error* (E4 palpable, E5 purple-prose sunset, E6 dangler) or a genuine micro-echo (clipboard/clipped) — never a registered device instance. V1–V6 and all ch1/ch2 registered device spans are untouched across both models (`strong-audit.json` `device-flagged` all false; verified manually). No flattening suggestion exists.

## Per-dimension scores

**D1 Functionality — 6.5.** Core voice-editorial loop completes well (6 qwen + 4 opus line-edit sessions, discuss, apply, dismiss→memory). But multiple peripheral journeys break: first chapter-create 500'd (`phase0.json` `04-ch1-create`), setup wizard-finish silently no-ops (`setup07-probe.json`), ghost text ~50% empty (`ghost-text-probes.json`/`-retry.json`), and an outage produced a fake-success. Works, with real broken edges.

**D2 Reliability & data safety — 6.5.** Strong: suggestions never auto-applied (chapter byte-identical until explicit writer action, `apply-and-redismiss.json` shows explicit apply→v2), exact-span optimistic versioning, X5 concurrent-edit race handled with no crash/stale-anchor (`x5-turn2` `span_rewrite_put_status:200`, thread stayed coherent), honest outage error copy. **Drag: D-36 dishonest failure state** — no words lost, but a never-edited chapter is presented as done (see D7). No export exercised (truncation criterion unevidenced).

**D3 Usability — 6.0.** Plain-language outage copy and graceful malformed-finding recovery are good. But: wizard toasts "Setup complete!" while `setupComplete` stays false (`setup07-probe.json` 32-settings-after); dismiss 400 leaks raw ZodError internals (`memory-around-dismiss.json` `details.name:"ZodError"` with full issue tree); dismiss `reason` silently dropped on a 200; discuss can't see chapter content so it must ask the writer to paste (`x5-turn2`). Multiple state-visibility and error-legibility defects.

**D3b Ergonomy & efficiency — 6.0 (partial; API-level).** Evidenced dead-ends: retry-after-500 on chapter create hits the ghost row and 500s again (D-37, code-cited non-atomic), and the setup-complete wall has no honest exit. Little positive keyboard/click-path evidence (HTTP-level bundle), so this is scored on the dead-ends present.

**D4 Onboarding / time-to-first-word — 5.0.** Manuscript paste (5 chapters, byte-identical round-trips) and fingerprint capture worked and the fingerprint was high-quality. But for THIS persona (paste-a-finished-manuscript stylist) the SETUP-07 guard 422-walls every agent workflow, and the wizard "Finish Setup" lies about completing it (D-35). "Offers never wall" is violated with a success-that-lies; only an out-of-band bible+architecture run (~18 min) cleared it. Serious onboarding-flow trust defect for the exact buyer.

**D5 Performance feel — 6.0.** Honestly instrumented, dev-server/single-worker labeled. But felt latency is mediocre-to-poor: discuss 18–127s (87.8s and 126.7s are "feels broken"), line-edit 109–323s, ghost text 1.5s(empty)/28–44s(substantive) — both unusable inline. Opus line-edit costs $2.31–$3.59/chapter ($10.21 for 4). Queue-honesty is breached by D-36 (reports success on nothing).

**D6 Look & feel — NO-EVIDENCE.** API-level bundle; no theme/contrast/locale/empty-loading state evidence. Only look-adjacent data is error copy (outage message good; ZodError leak bad) — insufficient to score the visual dimension.

**D7 Trust & safety — 5.5.** *Fundamentals sound:* invalid Anthropic key rejected at validation (400) and NOT stored, keys masked (`sk-or-v...705e`, `sk-ant-...jgAA`), clean override revert to null (`model-override-revert-and-keys.json`); SETUP-07 gate fires; no auto-apply; D-33/D-34 malformed-finding → graceful `REJECTED: Paragraph 9 does not exist…` with analytics row (`findings-after-lineedit-ch5-pass2.json` `594ea427`), zero raw errors. *But the money/state-honesty surface is materially compromised:* **D-43** editor-model override never governs line-edit — a BYOK user who sets editor=opus is silently billed qwen (`strong-setup.json` override 200 vs journey's qwen billing); **D-44** per-key usage panel shows `{totalTokens:0,totalCost:0,sessionCount:0}` after ~$0.39 real spend (`strong-setup.json` keys-before) — the cost-transparency *moat* lies; **D-35/D-39** success responses lie (setupComplete + dismiss-reason silently dropped, 200); **D-36** fake-success would poison a billed batch digest. Injection containment not exercised for this persona. No key/ownership breach, but repeated honesty-of-state and billing-transparency failures on precisely the surface this BYOK-pro persona trusts.

**D8 Manuscript intelligence quality — 8.5.** The standout dimension, independently verified. **0/7 misquotes** (byte-checked). **6/6 signature-device survival on BOTH qwen3.6 and claude-opus-4-6**, including the two historically-hard devices (and-stack accretion, inference anchors); ch1/ch2/ch4 reports exempt them by name; **precision 6/6** (no V-instance flagged). Discuss **genuinely adapts and holds**: adapt case `ce63cb76` (18s, real reason + `suggestedConstraint`), hold-ground case `bdb80574` (87.8s, refuses the weak defense yet offers in-voice compromise "Behind the island the light failed at 18:40. Barometer steady."). **Memory causally honored**: discuss constraint → WriterMemory row `58cfd563` ("Preserve dangling modifiers that mimic cognitive slippage or fatigue.", findingId-linked) → honored on the next pass AND *across a qwen→opus model switch* (`strong-audit` E6 not re-flagged). Genuine catches: palpable AI-tell, purple sunset, opus's clipboard/clipped micro-echo. **Held below 9 by:** floor-level recall (qwen 3/6 — E1 double-"under", E2 "small"×3 echo, E3 "I saw that" filter-word all genuinely present in `owen-ch5-what-the-water-keeps.md` and all missed; opus caught FEWER, 2, at 3.5× cost); D-41 empty `newText` on the critical finding (rewrite-comparison renders nothing); D-40 fingerprint doc persisted with fabricated example quotes + glitch tokens (see defects).

**D9 Retention / habit — 6.0 (partial).** WriterMemory learning loop works and persists cross-session/cross-model (real habit moat). AuthorshipTracker is honest-by-construction (hides rather than fabricates "100% yours"). But D-44 usage panel is a *dishonest* health surface ($0 while spending), and no streak/heatmap evidence in this bundle.

**D10 Delight — 7.5.** Genuine exceed-expectation moments: the hold-ground discuss compromise that honors a weak defense while preserving voice; cross-model constraint persistence; reports that name-and-protect devices; ghost text that, when it fires, is genuinely in register ("and crusted with salt, as if the rope had been pulled tight in a panic and left to harden while the tide went out"). Docked because the delight is gated behind 87–127s waits and broken ghost economics.

**D11 Competitive edge — 7.0.** For a stylist whose incumbents are Grammarly/PWA (generic rule fixes that would flatten his deliberate danglers/fragments), ChatGPT/Claude one-shots, and Sudowrite Muse (writes but doesn't preserve HIS captured voice), the voice-preserving quote-anchored findings + discuss + learns-preferences loop is a real, switch-worthy moat — and this journey *empirically proves it* (6/6 device survival on two models, discuss adapts, memory honored). That's what lifts it above baseline. But the wounds for this exact buyer are real: model-identity dishonesty (D-43 attacks the "transparent BYOK" moat directly), onboarding wall (D-35 bounces a paste-in stylist in hour one), no deterministic grammar/craft-report net (PWA wins), non-deterministic recall (3/6), and broken inline ghost text (the Muse-competitor feature). Proven edge on the one axis he cares most about, wrapped in enough friction/dishonesty to prevent a clean switch.

## Defects I found / re-weighted (beyond or against the executor)

1. **D-36 fake-success is worse than the executor emphasized (S2, confirm).** I independently confirmed via `line-edit-ch5-pass1-sse-raw-reattach.json` (not just narration): the post-outage retry streamed `{"type":"complete","content":"Session completed successfully","metadata":{"findingsCreated":0,"statusAdvanced":true,"newStatus":"line_edited","tokensInput":0,"tokensOutput":0,"costUsd":0,"endReason":"natural","suggestedNext":["beta-read"]}}`. The chapter is not just marked done — it is **advanced to `line_edited` and offered for beta-read**, so the fabricated success propagates to the next pipeline stage. Executor's S2 is right; the beta-read escalation makes it arguably S1-adjacent in a batch context.

2. **D-40 fingerprint fabrication — confirmed in `fingerprint.md`, mild under-rating (S4, but note the class).** Visible mid-sentence glitch tokens in a writer-facing document ("nicht by training" L5, "asnaczynią" L139, "无意" L222, "die-sel" L51, "semdp-wisdom" L216, "internal-monoologue" L268) and fabricated "example quotes" not in the corpus ("He was a man who set you waiting for the light to change" L121; "the feeling is named: it is late-stage grief" L240) plus a near-quote ("I lift the kerosene" vs corpus "I lifted"). It does NOT trigger the D8 finding-misquote cap (these are advisory fingerprint examples, not findings), but a fabricated quote attributed to the author's own corpus is a manuscript-intelligence integrity blemish. Executor's S4 is defensible; I flag it as the one place fabrication touched writer-facing output.

3. **Recall regression on the stronger model — under-narrated anti-value signal.** Opus (claude-opus-4-6, ~$3.59/chapter) caught FEWER planted errors (2) than qwen3.6 (3) on the comparable set. Spending 3.5× more did not improve editorial recall. The journey frames this neutrally ("gate-driven not model-driven"), which is fair, but it's a real D5/D11 negative: the premium path costs more for no recall gain. Confirmed against `strong-audit.json`.

4. **D-43/D-44 are more than generic S3s on the trust axis.** Both directly undermine the competitive-teardown's named moat "honest, transparent BYOK economics." D-44 (usage panel $0 while spending) and D-43 (paid model choice silently misrouted; no persisted per-session model provenance) mean the one surface a BYOK-pro trusts most is unreliable. I weight these into D7 heavily, not as peripheral.

5. **No misrepresentation found in the executor's core claims.** 0 misquotes, 6/6 device survival, memory causal chain, discuss adapt/hold, single-worker, and chapter byte-integrity all held under my independent verification. If anything the bundle is self-critical (honest recall 3/6, N=3-hunk sample caveat, BLOCKED-ENV honesty, ghost-economics honesty).

## "Suspiciously clean?" analysis
**No — this bundle is the opposite of suspiciously clean, and that raises its credibility.** The failure evidence I would expect from a real product is all present: a 500 on first chapter create, a genuine provider outage, a fake-success, an onboarding wall, broken ghost text, ZodError leaks, silent key drops, a $0 usage panel. The executor documented 10 defects (2× S2, 6× S3, 2× S4) against the very product it's grading, and pre-registered a discriminative probe (6 planted errors) specifically to disprove the "6/6 survival = lobotomy not precision" objection — and the editor DID catch 2–3 of the 6, answering it honestly. What's genuinely missing (not hidden, just unexercised): (a) injection-containment for this persona (a listed D7 sub-criterion — untested here); (b) any export/truncation test (D2 criterion untested); (c) UI/visual evidence (D6 unscoreable). One place the metric is massaged favorably: the opus leg excludes E6 from the recall denominator ("2/5 eligible") because the WriterMemory constraint suppressed it — legitimate, but it flatters opus's already-worse recall. The `misquote-probe-all.json` is an executor-authored summary of verbatim-ness; I did not trust it and verified independently — it held.

## One-line overall impression
A high-integrity, exhaustively-instrumented bundle whose manuscript-intelligence moat is genuinely best-in-class and verified clean (0/7 misquotes, 6/6 device survival on two model classes, discuss that holds-with-compromise, memory honored across a model switch) — but wrapped in a materially compromised trust surface (onboarding wall that lies, outage fake-success advanced to beta-read, silent paid-model misrouting, $0 usage panel) and an editor that now under-edits (recall 3/6), so the voice-preservation is switch-worthy while the surrounding product is not yet trustworthy.
