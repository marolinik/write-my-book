# P5 Wave C — browser/mobile capture on fixed build (d51514c)
2026-07-25 UTC (local 07-26) · dev :3001 (port 3000 occupied by foreign app; classifier blocked -p 3002 variants) · phone viewport 390x844
Persona: Sam `user_qa_p5` (user `d68c3b5e`), DEV_CLERK_ID flip + server boot. Worker up (agent-sessions, batch-digest, concurrency 2).
Book `35ff1112` "Sam Free-Tier On-Ramp QA 2026-07-20", Ch.1 `10334dac`. Default model `openrouter-qwen36/sonnet` (seeded reasoning default). Infra: compose postgres/redis/neo4j/qdrant up; minio NOT started (port 9000 held by platform-new-minio-1; no S3 path in this wave).

## Item 1 — Phone ghost-text render (D10) + loading state (D5) + latency (D-128)
- Ghost text OFF by default; enable lives in editor "More tools" menu ("Enable AI Ghost Text") — `04-phone-more-tools-menu.png`.
- **RENDERED on phone**: typed "…wrote about the", 1.5s pause → POST ghost-text 200 → overlay suggestion *"dream: the desk was a ship, and the words were its wake in the blank sea of the page."* in muted italic + "Tab ↹" hint — `06-phone-ghost-text-rendered.png`. Second render: "…knock at the" → "door." — `08-phone-ghost-warm-second.png`. Autosave stamp visible both shots ("Saved 12:01 AM" / "Saved 12:06 AM").
- **Tab accept works** — suggestion inserted into doc (`07-phone-ghost-accepted-tab.png`) **BUT joins without leading space**: doc now reads `"wrote about thedream: the desk was a shi…"` (exact bytes verified via DOM). → **NEW D-130**.
- **No loading affordance** during the 1.5s debounce + 3–6s fetch: no spinner/shimmer anywhere (`05-phone-ghost-loading.png` = editor identical to idle). Component has no loading state at all (`ai-ghost-text.tsx` — only internal pending refs). D5 evidence: the wait is blind.
- Latency (PerformanceResourceTiming, one page session): cold **3308ms**, warm **4301ms**, **6209ms**. Warm NOT faster than cold tonight → route-compile is not the dominant term; upstream inference variance dominates. D-128 stays measure-class; prod sample still open.

## Item 2 — Cap-exhaustion wall (D5/D7) — Sam's 4-rounds-unprobed fear
Method: `free_tier_usage` ghost_text_calls set 3→100 (limit `FREE_TIER.dailyGhostText=100`), typed to trigger.
- **API honest**: POST ghost-text → **429** `{"error":"Free plan includes 100 ghost-text completions per day. Resets at midnight UTC.","upgradeToTier":"indie","remainingToday":0}`. Not 500, not silent server-side. Meter did not advance on rejection.
- **UI silent**: NOTHING renders — no toast, no banner, no upgrade nudge; ghost simply stops appearing (`09-phone-cap-wall-moment.png` = plain editor). Cause: `ai-ghost-text.tsx:81` `if (!res.ok || aborted) return;` swallows every non-200. → **NEW D-129** (systemic, see Item 5).
- Counter reset to 3 after capture.

## Item 3 — Cross-tenant isolation under Sam auth (D7)
As Sam (browser session), against P1's book `4116055c`:
- GET /api/books/:id → **404** `{"error":"Book not found"}`
- GET /api/books/:id/documents → **404** same envelope
- POST /api/books/:id/ghost-text → **404** same envelope (no spend, no leak)
- Page /books/:id → app-rendered **404 "Page not found"** + "Go to dashboard" (`10-phone-cross-tenant-book-page.png`). Existence-hiding 404 (not 403) — PASS.

## Item 4 — Billing panel (D-119) + D-127 disclosure state
- `11-phone-billing-panel.png` (full page): founder slots "0 of 200 claimed · 200 left" consistent; BYOK spend **$0.03** (sub-cent formatter live, not $0.00); usage: 12 agent sessions ~$0.03, 20 sessions, 3.2K in / 10.6K out tokens.
- D-127 (point-of-use substitution disclosure): still **absent** — no disclosure in editor, More-tools menu, or ghost overlay.
- **NEW D-131**: Settings "Global Default Model" combobox renders **BLANK** for Sam's stored `openrouter-qwen36/sonnet` (`12-phone-settings-model-selection.png`). Root cause `model-picker.tsx`: displayName dedup keeps only highest tier id (`openrouter-qwen36/opus`), so the stored sonnet id has no SelectItem → Radix shows empty trigger. Sam cannot even SEE his default, let alone the substitution. D-127 family, worsens it.

## Item 5 — D-118 422 backstop copy
- Source verified `quick-assist.ts:46/48`: copy now split per surface (ghost-text variant says inline edit still works; inline variant scoped to inline). D-118 overscope FIXED in copy.
- **But unreachable**: both quick-assist surfaces discard all error bodies — ghost `ai-ghost-text.tsx:81` (`!res.ok → return`), inline `inline-edit-popup.tsx:157` (`catch → setPhase("instruction")`), no hook-level onError/toast (`use-inline-edit.ts`). The honest 422 AND 429 copy cannot reach any user. Live-forcing a true 422 (upstream reasoning-only despite substitution) not attempted — cost + nondeterminism; copy-render verification is moot until D-129 fixed.

## New defects
| ID | Sev | Summary |
|---|---|---|
| **D-129** | S2 | Quick-assist clients swallow ALL non-200s (ghost + inline): 429 cap-wall copy, 422 backstop copy, 5xx — user sees silence. `ai-ghost-text.tsx:81`, `inline-edit-popup.tsx:157`. THE D5/D7 floor evidence: honest server, mute client. |
| **D-130** | S3 | Ghost Tab-accept inserts suggestion without leading space → word-join corruption ("thedream"). |
| **D-131** | S3 | Model picker blank for stored non-opus tier of deduped family (qwen36/sonnet); user can't see own default. D-127 family. |

## Verdict for re-judge binder
D10 phone render: **SUPPLIED** (ghost as-you-type, accept, autosave, mobile nav). D5: wait is blind (no loading state) AND wall is silent in UI (D-129) — floor evidence now precise + fixable small. D7: cross-tenant clean 404s; wall API honest. D-118 copy fixed but dead-ended by D-129. D-127/D-131 disclosure cluster open. Fix lane next: D-129 (surface error copy), D-130 (space join), D-131 (picker value), D-127 (disclosure line) — then re-shoot 09 (wall moment with visible copy) + ghost render on fixed build.

---

## RE-SHOOT 2026-07-26 — fixed build (bdb45c1 + ca8d0c9), phone 390x844 DPR3, headless Playwright
All four fix-lane defects verified DEAD live; evidence for re-judge v5.

| Shot | Proves |
|---|---|
| `13-phone-more-tools-disclosure.png` | **D-127 CLOSED**: "Enable AI Ghost Text" menu item carries "Quick suggestions may use a faster model than your default." |
| `14-phone-ghost-loading-dots.png` | **D5 affordance**: muted pulsing `···` (`role=status`) at cursor during in-flight fetch — the wait is no longer blind. |
| `15-phone-ghost-rendered.png` | Ghost render on fixed build: italic suggestion + "Tab ↹", autosave stamp "Saved 02:18 AM". |
| `16-phone-ghost-accepted-join.png` | **D-130 CLOSED**: doc reads "wrote about the ship and its strange cargo…" — space join (DOM-verified `OK-NO-GLUE`); pre-fix "thedream" glue still visible earlier in the same paragraph as historical contrast. |
| `09b-phone-cap-wall-toast.png` | **D-129 CLOSED**: at ghost_text_calls=100, toast renders the server's honest copy "Free plan includes 100 ghost-text completions per day. Resets at midnight UTC." + **Upgrade** action. Silence is dead. |
| `18-phone-upgrade-deeplink-billing.png` | Upgrade action deep-links to `/settings/billing` (page loaded). |
| `17-phone-inline-popup-disclosure.png` | **D-127 second surface**: AI Edit popup instruction phase shows the disclosure line under Generate/Cancel. |
| `19-phone-settings-picker-fixed.png` | **D-131 CLOSED**: Global Default Model trigger renders "Qwen 3.6 27B (OpenRouter) $$" for Sam's stored sonnet id (was BLANK in shot 12). |

Persistence probe (no shot): Tab-accepted text + 12s autosave flush + fresh reload → **SURVIVED** ("counted the strokes…" back after reload, space join intact). The earlier apparent loss of run-1's accepted sentence was the capture harness closing the browser inside the autosave debounce — harness artifact, NOT a product defect (no new D-number).
Counter restored: free_tier_usage 2026-07-26 ghost_text_calls set 100 → reset (typed-probe traffic only). Capture tool: repo Playwright (MCP browser tools unavailable this session); script preserved in session scratchpad `p5-reshoot.js`.

---

## RE-CAPTURE v6 — 2026-07-26, fixed build `5ea1700` (D-132/133/134/135 + panel refinements), phone 390x844 DPR3 + isMobile + hasTouch, headless Playwright
Context change vs v5: `isMobile: true, hasTouch: true` so `(pointer: coarse)` matches — the D-132 tap affordance is coarse-gated and would be invisible in the v5 context. Dev :3001, DEV_CLERK_ID→user_qa_p5 (restored after), fresh server boot.

| Shot | Proves |
|---|---|
| `20-phone-ghost-tap-hint.png` | **D-132**: ghost overlay renders with visible "Tap to accept" pill on coarse pointer (fine pointer keeps passive overlay + "Tab ↹"). |
| `21-phone-ghost-tap-accepted.png` | **D-132 CLOSED on camera**: `page.touchscreen.tap` on the overlay (REAL touch, no Tab key) → suggestion INSERTED, `JOIN-CHECK: OK-NO-GLUE` (D-130 join intact), 13s autosave + reload → `PERSISTENCE: SURVIVED`. |
| `22-phone-cap-wall-banner.png` | **D-134**: 429 wall renders persistent `role=alert` banner, top-anchored (keyboard/bottom-nav-proof), verbatim server copy "Free plan includes 100 ghost-text completions per day. Resets at midnight UTC." |
| `23-phone-wall-reentry-banner.png` | **D-134 re-entry CLOSED**: second typing pause inside the old 60s cooldown window — banner STILL VISIBLE (v5's re-silenced wall dead). |
| `24-phone-wall-upgrade-billing.png` | Upgrade button (measured **89x44px**) deep-links `/settings/billing`. |
| `25-phone-wall-dismissed.png` | Dismiss (**44x44px**) clears the banner; instrumented probe: post-dismiss re-type produced **ZERO further ghost requests** (wall suppression `WALL_RETRY_MS`=5min — no doomed per-pause 429s, no banner bounce-back). |
| `26-phone-flushclose-survived.png` | **D-133 CLOSED on camera**: typed probe sentence, page closed **700ms after last keystroke** (inside the 2s autosave debounce), then a **FRESH browser context** (no IDB/localStorage recovery possible) reloaded → `FLUSH-SURVIVAL: SURVIVED-SERVER`. Only the pagehide keepalive PUT can explain it. |
| `27-phone-422-backstop.png` | **D-118 backstop rendered live**: honest 422 toast with verbatim copy ("This model returns only internal reasoning at ghost-text's tiny budget…") + **Open Settings** action — the missing capture v5 judges named. |

### Staging disclosures (protocol per v5 wall staging)
- Wall: `free_tier_usage.ghost_text_calls` staged 6→100 for the wall shots, restored after; final honest value **7** (4 pre-session + 2 tap-mode successes + 1 probe success; 429s/422 never increment — meter integrity held on camera again).
- 422: unreachable for ANY real config today (every provider has a fit haiku-tier quick-assist fallback — defense-in-depth). Forced by temporarily flagging all 21 OpenRouter registry entries `unfitForQuickAssist` (source staging, `git checkout` reverted, tree clean), making resolution fall back to the flagged qwen36 whose thinking survives the reasoning-disable → genuine server 422 through the real pipeline with real copy.
- **Correction to prior-session note**: `openrouter-qwen-max/*` is NOT a latent unfit candidate — under `withQuickAssistReasoning` it returned real text (200, 13 output tokens, usage-recorded). The unfit flag remains correctly qwen36-only.

### Harness artifacts (disclosed, not product defects)
- Dangling "the" fragments visible in wall-run prose (shots 22/23/25) are typed trigger sentences the wall rejected (no ghost output involved) — same class as the v5 D8 artifact; chapter `10334dac` re-cleaned through the editor (select-all + retype + autosave) before and after the wall/422 runs; final state = clean 4-sentence baseline + tap-accepted continuation.
- Headless Clerk-js CDN failures intermittently dropped the session mid-run: one ghost POST was rewritten to an HTML 200 (222ms, no route execution — no usage record, no meter change), whose JSON-parse failure raised the transient fallback error toast; this produced one misleading "banner reshown" probe reading. Instrumented re-run (request-logged): exactly ONE ghost request, one 429, banner behavior correct. Direct API probe at the wall: honest 429 in 69ms.
- Red "1 Issue" pill occluding Home/Books in bottom nav (shot 23) = pre-existing **D-136**, untouched by this lane.

---

## RE-CAPTURE v7 — 2026-07-26, fixed build `a97d025` (D-138 overlay clamp/flip), phone 390x844 DPR3 + isMobile + hasTouch, headless Playwright
Same phone context as v6. Dev :3001 (hot-reloaded fix — no server reboot needed), DEV_CLERK_ID→user_qa_p5 (left as Sam: capture waves continue this session). Script: session scratchpad `d138-capture.js` (session 61796695). Caret driven to the right edge by per-char typing with live `getSelection()` rect measurement.

| Shot | Proves |
|---|---|
| `28-phone-ghost-edge-flip.png` | **D-138 CLOSED on camera**: caret mid-word at x=261 (available 121px < 160 threshold) → overlay **FLIPS to next line**: measured `left=8px`, `top=476 == caret.bottom` exactly, `computed maxWidth=374px == 390−2·8` exactly. Ghost renders as full-width wrapped prose lines ("…across t / he dark water, and in its momentary pass…" — seamless mid-word continuation), **"Tap to accept" pill fully in-viewport** (measured 91x20 at y=566). Pre-fix this exact caret produced a ~30px one-word-per-line column burying the pill (v6 shot 20 class). |
| `29-phone-ghost-landscape-reclamp.png` | **Rotation reclamp (secondary)**: after `setViewportSize` 844x390 the resize listener re-runs placement. Run-1 numeric proof (logged): overlay re-anchored **INLINE at caret** `left=655px`, `maxWidth=180.578px == 844−8−655.42` — exact remaining-room arithmetic, flip did not persist. Shot (run-2, after scroll-into-view) shows the landscape ghost full-width at the fold; caret sat at viewport bottom post-rotation so the visual is partial — vertical tracking is the registered D-138-RESIDUAL-VERTICAL edge case (pre-existing class, refuted as regression in the fix lane). |

Notes:
- Two capture runs (run-2 re-shot 28 with the scroll fix); each run typed one filler sentence + accepted no ghost — chapter `10334dac` grew by the typed probes only (billable ghost calls: 2 successes + retries within free tier; meter honest).
- Session start required flipping `DEV_CLERK_ID` back to `user_qa_p5`: Next dev hot-reloads `.env`, so the S3 post-session restore to `user_dev_bypass` had (correctly) demoted the live server to the dev tenant → Sam's book 404'd until re-flipped. Worth remembering: the "server still runs Sam env" assumption does not survive `.env` edits.
- "2 Issues" pill occluding bottom nav (shot 28) = pre-existing **D-136**, untouched.

### v7 addendum — D-139 toast re-anchor on `7657588`
| Shot | Proves |
|---|---|
| `30-phone-422-toast-top.png` | **D-139 CLOSED on camera**: same staged-422 protocol as v6 shot 27 (all-OpenRouter `unfitForQuickAssist` source staging, reverted after — tree verified clean), same real server 422 with verbatim copy + **Open Settings** action; toast now renders **top-center** (`data-y-position=top`, measured top=80px ≈ the 5rem chrome offset) instead of bottom-right under the tab nav/keyboard. Desktop untouched (bottom-right, sonner defaults). First shot under the **v8 protocol**: `nextjs-portal` hidden (D-136 adjudicated as Next dev-tools chrome — see `fix-reviews/D-136-adjudication-nextjs-dev-indicator.md`). |

### v7 addendum — D5 Stage 1 streaming on `01192a3` (tokens flowing ON CAMERA)
| Shot | Proves |
|---|---|
| `31a-phone-ghost-stream-first-tokens.png` | First streamed tokens ("the black beyond,") render faded-italic at the caret with the streaming caret — **before** the suggestion is complete. |
| `31b-phone-ghost-stream-growing.png` | Mid-stream growth frame (len 56 of 155). |
| `31c-phone-ghost-stream-done-pill.png` | Done state: full 4-line suggestion wrapped inside the D-138-clamped width, **"Tap to accept" pill armed ONLY now** + honest measured **"~6.7s" timing chip**. Bottom nav fully visible (v8 protocol). |

Instrumented run-3 timeline (poll every 100ms): len **9 → 56 → 105 → 133 → 148 → 150 → 155(pill)** between t=5.5s and t=11.1s after last keystroke — progressive render REAL, `PILL-TIMING: pill only at final length (armed-on-done)`. Network: ghost POST answered `200 text/event-stream` with `Server-Timing: ttft;dur=1124` (warm). TTFT across 3 runs: **1.1s warm · 5.9s (first hit after hot-reload = dev route compile, D-128 class) · 17.0s (provider-side spike, OpenRouter free-tier qwen36)** — the spike is exactly the case the streaming + >8s still-writing affordance + timing chip make honest instead of silent. Wart logged for Stage 2: run-3 shows a ~4.4s gap between the last delta (len 150) and the done frame (len 155 + pill) — `finalMessage()` tail latency delays accept-arming.
Harness notes: chapter accumulated typed-probe fragments over 3 runs (same class as v6) — re-cleaned through the editor after the wave (`CLEAN-VERIFY: CLEAN`, baseline 4-sentence prose restored). Billable: 3 streamed successes on Sam's free meter (honest, increment-on-success).

---

## RE-CAPTURE v8 — 2026-07-26, D4 probe wave on `47474bb` (the v7 unanimous floor: model-comprehension/onboarding, unprobed 7 rounds)
Phone context + v8 protocol (portal hidden) throughout. Sam env (:3001, DEV_CLERK_ID=user_qa_p5).

| Shot | Proves |
|---|---|
| `32-phone-ghost-menu-disclosure.png` | **D-127 point-of-use disclosure ON CAMERA**: More-tools menu item reads "Enable AI Ghost Text — Quick suggestions may use a faster model than your default." (captured DOM text verbatim). |
| `33-phone-settings-byok-top.png` | BYOK first-run surface: Settings shows "API Keys — Bring Your Own Key (BYOK) — your API keys are encrypted at rest" with per-provider entries. |
| `34-phone-settings-model-picker.png` | **D-131 surface re-verified**: Default Model picker renders "Qwen 3.6 27B (OpenRouter) $$" for Sam's stored default (named model + cost band, not a blank/slot label). |
| `35-phone-billing-meter-before.png` / `35-phone-billing-meter-at-wall.png` | Usage & Billing on phone: plan cards + REAL per-agent meter (Ghost Text 19 sessions $0.00 3.7K in/512 out; Inline Edit 6 sessions $0.03) and per-model rows with true registry ids (deepseek/deepseek-v3.2 — the D-127 substitution visible IN THE METER). At-wall shot taken while staged to 100. |
| `36-phone-fresh-book-ghost.png` | **NON-SEEDED RUN**: brand-new chapter "Interior Weather" created through the product (201), fresh original prose typed by keyboard (Marta/parking-meters — different voice from the seeded lighthouse gothic), ghost STREAMED a voice-matched continuation: "touched the cool metal of its bent neck, as if trying to divine whether it had heard anything she ought to know." Kills the seeded-only-prose ding (D8/D6/D1). |
| `37-phone-first-pause-wall-banner.png` | **First-pause wall, instrumented** (v6 shot-22 remedy): request log shows exactly ONE ghost POST → 429 → persistent role=alert banner with verbatim copy "Free plan includes 100 ghost-text completions per day. Resets at midnight UTC." + Upgrade + dismiss. No toast ambiguity, portal hidden. |

Staging disclosures: meter staged 13→100 for shots 35-at-wall/37 (SQL, restored to 13 immediately after — honest value = 7 pre-session + 6 billable streaming/capture successes today). Everything else in this wave is UNSTAGED. Bonus unstaged wall found live: `POST /api/books` as Sam → **403 "Free plan includes 1 book. Upgrade to Indie…"** (honest book-cap wall with upgradeToTier; the non-seeded run therefore lives in chapter 2 of the existing book — disclosed).

### v8 audit re-shoot (post-aggregate, same build)
| Shot | Proves |
|---|---|
| `32b-phone-ghost-disclosure-inpixel.png` | v8 audit ding #1 remedied: ghost menu item scrolled into view — the D-127 disclosure line "Quick suggestions may use a faster model than your default." measured in-viewport (`disclosureInView:true`), no DOM-read assertion needed. |
| `35b-phone-billing-usage-by-model-deepseek.png` | v8 audit ding #2 remedied: Usage-by-Model scrolled to the substitution row "DeepSeek V3.2 (OpenRouter) (deepseek/deepseek-v3.2)" measured in-viewport (top=386, `inView:true`) — the D-127 substitution visible in the writer's own meter, in pixels. |

### v8 addendum — D-143/D-144/D-145 fix re-measure on `1de4ef0` (shots 31a-c SUPERSEDED in place)
Instrumented re-run on the fixed build (old 31a-c from `72b6338` remain in git history): timeline t=3206ms len 1 → t=3483 len 67 → t=3744 len 95 **with pill ARMED at 3744ms — 261ms after the prior growth poll**. The D-143 ~4.4s settle-tail dead-zone is GONE on camera (settle now derives from message_stop; done frame precedes billing writes). `Server-Timing: ttft;dur=1567` warm. D-144 preview join-space + D-145 felt-latency chip land in the same commit (test-pinned: preview===insert byte-identical; chip shows client-felt fetch→armed).
