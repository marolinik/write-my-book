# Key facts (exploration, 2026-07-03)

- Workflow ids (workflows.ts): new-novel, capture-style, create-story-bible, build-architecture, coach, onboard-new-book, onboard-imported-book, read-manuscript, discuss-chapter, plan-chapter, write-chapter, freewrite, free-drive, dev-edit, line-edit, beta-read, revise, discuss-edits, publishing-check, analyze, market-analysis, refresh-style, evolve-style, research-world, research-topic, init-series, create-series-bible, create-series-architecture, check-series-continuity.
- Dispatch rule (agent/route.ts:374): background BullMQ iff !workflow.conversational && estimatedMaxMinutes>5; conversational and short workflows run inline in-process with fire-and-forget promise.
- Every book workflow spawns agentType 'writing-coach' as conductor; the requested workflow's primaryAgent only reaches specialists through the DelegateToSpecialist tool with {agentType, task, chapterNumber, workflowId}.
- Coach model = `${resolvedSpecialistProvider === 'openrouter' ? 'openrouter' : provider}/sonnet` — NOT the user's coach-role model; specialists resolve honestly via resolveModelForRole(role, bookSettings, globalRoleOverrides, userDefault) with 4-level chain and 'default'/null skipping.
- AgentType→role map (model-resolver.ts): ghostwriter→ghostwriter; dev/line-editor→editor; beta-reader→beta-reader; style/manuscript-analyst, continuity-checker, manuscript-reader, world-researcher, market-reader, publishing-editor→analyst; writing-coach→coach; story-architect, scene-planner→creative.
- qwen3.6 registry ids: openrouter-qwen36/opus|sonnet|haiku all → modelId 'qwen/qwen3.6-27b', provider 'openrouter', $0.285 in/$2.4 out per 1M; OpenRouter Anthropic-compat baseURL is 'https://openrouter.ai/api' (SDK appends /v1/messages).
- Orchestrator constants: MAX_TOOL_TURNS=50, max_tokens=64000 (hardcoded every call), DEFAULT_MAX_RUNTIME_MS=30min, DEFAULT_MAX_SESSION_COST_USD=$10, APPROVAL_TIMEOUT_MS=10min, WARNING_THRESHOLD=0.8; session budget from route = max(2×estimateWorkflowCost(workflowId, coachRegistryId).max, $5); runtime ceiling = (estimatedMaxMinutes+30)min.
- Stream handling forwards ONLY text deltas and tool_use starts; no 'thinking' branch exists anywhere in src/lib (grep confirms); finalMessage.content (including any thinking blocks) is pushed raw into messages history each turn.
- Final-turn wrap-up allowlist WRAP_UP_TOOLS = {CreateFinding, WriteDocument} (orchestrator.ts:609) — WriteChapter, DelegateToSpecialist, RequestApproval excluded by design; tool results >50000 chars truncated; SSE tool_result preview truncated at 500 chars.
- CreateFinding validation: fuzzyMatch(anchorQuote, chapter) ≥0.8 else REJECTED row persisted with rejectionReason; paragraph auto-correct if quote found elsewhere (≥0.5); dedup via contentHash(chapter,category,description); groundingScore = 0.7×anchor + 0.3×avgAlternatives; finding statuses: pending→applied(appliedAt)/dismissed(rejectedAt)/rejected(validation).
- Session persistence: AgentSession row (status running/completed/failed, tokensInput/Output, actualCostUsd, estimatedCostUsd, jobId for background); Redis keys session:{id} (pub/sub), session:{id}:messages (RPUSH, 24h TTL), session:{id}:status, session:{id}:cancel, approval:{approvalId}.
- ConversationTurn persistence is dead: addUserMessage/addAssistantMessage (session-manager.ts:142,162) have zero callers; /message route continuation therefore starts from empty history except in-memory turns accrued by reference mutation after the first /message call.
- Post-session status advance map: discuss-chapter→discussed, plan-chapter→planned, write-chapter→drafted, dev-edit→dev_edited, line-edit→line_edited, beta-read→beta_read (+beta_passed if gate PASSED); forward-only via transaction; circuit breaker blocks auto-revise after 3 failed beta reads.
- Tier-1 prompt sections and priorities: chapter_content=100(never trim), book/language/scope=98, writer_memory=90, book_meta=85, story_bible=80, architecture=70, fingerprint=60, voice_metrics=58, calibration=57, plan/brief=55, finding_history=50, knowledge_graph=45, series=40/38/36, adjacent_chapters=30, craft_skills=25, relevant_memory=20, insights=15, session_continuity=14, page_context=10; budgets: dev-editor/continuity/coach 150k, ghostwriter 120k, beta-reader 100k, line-editor/manuscript-reader 80k, others 60k; estimator = chars/4.
- Vector memory: OpenAI text-embedding-3-small via platform env OPENAI_API_KEY only (isEmbeddingAvailable); getRelevantMemory limit 5, scoreThreshold 0.35, 3s timeout, excludes current chapter; indexing fire-and-forget in post-session.
- Provider fallback chain (client-factory resolveProviderRoute): anthropic→direct else OpenRouter; openrouter→OpenRouter only; openai/gemini/grok→LiteLLM proxy (LITELLM_BASE_URL, default localhost:30400) else OpenRouter with model-id translation (google/, x-ai/ prefixes); route 'none' throws — never platform-key fallback.
- Retry: 3 attempts, backoff 10/30/60s on 429/500/502/503/529; 400/401/402/403/404 throw immediately as ProviderError with translated user message; user cancel mid-stream records partial turn tokens from message_start/message_delta usage events.
- Background worker: src/worker.ts (npm run worker:dev; separate Dockerfile.worker), BullMQ Worker concurrency 2, lockDuration 5min; job data carries coachRegistryId+coachModelId+providerKey+sessionCostLimit (normalizeSessionCostLimit guards Infinity→null); keys always re-fetched from DB, never serialized into jobs.
- SessionBrief only created in agent-worker.ts onComplete when endReason ∈ {budget,timeout} (uses wrapUpSummary); prompt-assembler injects last 3 briefs; deploy gates pending per project memory: prisma db push for ContinuityFlag (4.4) and FindingReply.role + WriterMemory.findingId (4.2).
- Chapter save API: PUT /api/books/[id]/chapters/[chapterId]/content with {markdown, expectedVersion?, changeSource?} → {wordCount, version, bookWordCount}; 409 returns {currentVersion, serverContent}; GET returns {markdown, wordCount, documentId?, version?}. Client no-op-adopts identical-content 409s after sanitizeUnicode (U+FFFD→em-dash) on both sides.
- Chapter PATCH /api/books/[id]/chapters/[chapterId] accepts only {title, status, actNumber, chapterNumber, betaGate, betaScore} (updateChapterSchema, src/lib/validation.ts:57) — NO targetWordCount. Chapter model has @@unique([bookId, chapterNumber]) (prisma/schema.prisma:200), so any reorder must be transactional/two-phase.
- Corkboard reorder targets PATCH /api/books/[id]/chapters/reorder which does NOT exist — the only chapters routes are route.ts (GET/POST) and [chapterId]/route.ts (GET/PATCH/DELETE) plus [chapterId]/content and [chapterId]/branches.
- Continuity (4.4): POST /api/books/[id]/continuity/scan?chapterNumber=N → {flags: ScanFlag[], degraded?}; degraded=true keeps prior client flags. POST /api/books/[id]/continuity/intentional {flagId}. Idle trigger: scan on chapter switch + 20s after last edit (editTick counter). DEPLOY GATE: ContinuityFlag table (schema.prisma:401) needs `prisma db push`; same gate for FindingReply.role + WriterMemory.findingId (4.2).
- AI endpoints: POST /api/books/[id]/ghost-text {context: last 500 chars, chapterNumber, maxTokens: 60}; POST /api/books/[id]/inline-edit {selectedText, surroundingContext (±500 chars with [SELECTED TEXT] marker), instruction?, count: 3} → {suggestions: [{label, text}]}.
- useApplyFinding/useDismissFinding accept string | {findingId, alternativeIndex?, overrideText?} / {findingId, reason?} — both call shapes in the codebase are valid.
- Editor state: per-pane zustand stores keyed 'primary'/'secondary' (getOrCreatePaneStore in src/stores/editor-store.ts) + useActiveEditorStore for split mode; annotation ids are 'finding-<uuid>' and 'continuity-<uuid>'; loadEpoch + chapterId guard stale in-flight saves.
- localStorage keys: wmb-book-view-<bookId> (list|canvas|pipeline|corkboard), wmb-conflict-draft-<chapterId> (conflict safety draft), wmb:legend-seen; onboarding dismissed/toasted state per book via lib/onboarding/local-state.
- Chapter status pipeline: undiscussed → discussed → planned → drafted → dev_edited → line_edited → beta_read → beta_passed; betaGate: pending|passed|near_miss|failed. Sidebar/overview 'next workflow' heuristic: capture-style → create-story-bible → build-architecture → per-chapter discuss/plan/write → dev-edit/line-edit/beta-read → discuss-edits → publishing-check.
- Onboarding word-count plumbing: save response bookWordCount is published via qc.setQueryData(['book-wordcount', bookId], n); useOnboardingOffers watches crossings and fires toast offers (10s, with 'Not now' dismissal).
- Book creation: POST via useCreateBook returns book.firstChapterId — '/books/new' 'Start writing' goes directly to /books/[id]/chapters/[firstChapterId]; 'Guided setup instead' goes to /books/[id]/setup?step=1.
- Keyboard shortcuts in editor: F2 inline AI edit (selection required), F8/Shift+F8 finding navigation, Tab accepts ghost text, Ctrl+K command palette, Escape exits immersive; toolbar has roving tabindex; shortcut ownership resolved per pane in split view via EDITOR_PANE_ATTR.
- Redirect routes: /books/[id]/documents → /library; /wiki → /library?tab=wiki; /import → /transfer; /export → /transfer?tab=export. Sidebar 'Export' nav links to /transfer?tab=export.
- prisma/ contains only schema.prisma (no migrations dir) — schema changes deploy via `prisma db push`. Recent commits (32bfc05, 76d4a66, 3107f73) show Tier 4.8 Shelf shipped and OpenRouter model work landed after the session-start snapshot.
- Test surface: tests/unit (26 files incl. continuity-*, shelf-*, finding-conversation, onboarding-*) and tests/e2e (Playwright: offline-autosave, mobile-editor, inline-edit, editorial, documents, a11y, smoke).
- Dead-but-present code to either wire or delete: editor/finding-review-mode.tsx, editor/version-branching.tsx (branches API route EXISTS at /api/books/[id]/chapters/[chapterId]/branches), editor/entity-mention-popup.tsx, editor/describe-your-change.tsx, book/story-health-dashboard.tsx, book/completion-forecast.tsx, book/writing-achievements.tsx, book/shareable-progress-card.tsx, book/word-sprint.tsx, book/book-overview-tabs.tsx.
- Autosave contract: PUT /api/books/:id/chapters/:chapterId/content {markdown ≤2M, expectedVersion?, changeSource?} → 200 {wordCount, version, bookWordCount}; stale → 409 {error:'version_conflict', currentVersion, serverContent}; GET → {markdown, wordCount, documentId, version}. Omit expectedVersion = last-write-wins. Always stamp the returned version for the next save.
- POST /api/books {name,...} → 201 {...book, firstChapterId}; duplicate name 409; Chapter 1 pre-created empty (doc created on first content PUT).
- Agent start: POST /api/books/:id/agent {workflowId, chapterNumber?, message?, pageContext?} → inline {sessionId} OR background {sessionId, queued:true, jobId} when !conversational && estimatedMaxMinutes>5. Then GET .../agent/:sessionId/stream (SSE, Last-Event-ID resume, 15s keepalives). Gate order: 404 book → 400 workflow → 422 prerequisites{missing[]} → 422 setup guard → 429 quota → 422 minimumTier{blocked:true} → 400 missing/invalid provider key.
- Setup guard: non-setup workflows 422 {setupIncomplete:true, redirectTo:'/books/:id/setup'} unless settings.setupComplete OR (FINGERPRINT + STORY_BIBLE + ARCHITECTURE documents exist AND chapter count > 0). You can satisfy it by POSTing those three documents via POST /api/books/:id/documents.
- Workflow ids (28): new-novel, capture-style, create-story-bible, build-architecture, coach, onboard-new-book, onboard-imported-book, read-manuscript, discuss-chapter, plan-chapter, write-chapter, freewrite, free-drive, dev-edit, line-edit, beta-read, revise, discuss-edits, publishing-check, analyze, market-analysis, refresh-style, evolve-style, research-world, research-topic, init-series, create-series-bible, create-series-architecture, check-series-continuity.
- Background sessions REQUIRE Redis + the separate worker (npm run worker:dev). BullMQ: jobId=sessionId (dedupe), attempts 3, exp backoff 30s, lock 5min, concurrency 2. Messages replayed from Redis list session:{sessionId}:messages (TTL 24h); status at session:{sessionId}:status; approvals at approval:{approvalId} polled every 2s, 10-min timeout auto-reject.
- SSE 'complete' metadata (background): {findingsCreated, statusAdvanced, newStatus?, betaGateResult?, suggestedNext[], tokensInput, tokensOutput, costUsd, endReason natural|budget|timeout, wrapUpSummary?}. Budget cap = max(2×estimateWorkflowCost().max, $5); wall clock = (estimatedMaxMinutes+30) min; early end persists SessionBrief consumed by the next session's prompt.
- BYOK is mandatory: POST /api/settings/api-keys {provider: anthropic|openrouter|openai|gemini|grok, key} — live-validated against the provider before save; unvalidated keys are ignored by sessions. OpenRouter route = Anthropic SDK with baseURL https://openrouter.ai/api (SDK appends /v1/messages).
- Chapter prose S3 key: manuscript/act-{act}/chapter-{NN}.md (NN zero-padded); versions at .versions/{documentId}/v{n}.md; planning docs at .planning/STORY-BIBLE.md, ARCHITECTURE.md, FINGERPRINT.md, EXPORT-CONFIG.json; reports at .planning/chapters/chapter-NN-{DEV-EDIT|LINE-EDIT|BETA-READ}.md. Export compiles from the S3 glob manuscript/**/*.md — NOT from the chapters table.
- Export: POST /api/books/:id/export {format: docx|pdf|epub, isDraft?, sceneBreakGlyph?, template?} → {filename, storageKey, wordCount, chapterCount, estimatedPages, warnings[], format} (falls back to format:'md' with a warning if Pandoc fails). Download: GET /api/books/:id/export/:filename. Config: GET/PUT /api/books/:id/export/config (partial deep-merge). Requires pandoc on PATH (+typst for pdf) — both installed locally. Export is never plan-gated.
- Findings: GET /api/books/:id/editorial/findings?status=pending&chapterNumber=N&limit=200 → {findings[], total}. PATCH .../findings/:findingId {action: apply|dismiss, alternativeIndex?, overrideText?, reason?}: apply with originalText+newText edits the chapter in place (exact→fuzzy match; 409 if text drifted) and creates a doc version changeSource 'finding:{id}' WITHOUT optimistic lock. Discuss: POST .../findings/:findingId/discuss {writerMessage ≤2000}; 3-turn cap (409 {capped:true}), 200/24h rate limit (429).
- Import: POST /api/books/:id/import/preview (multipart files) → {chapters[{tempId,number,title,content,wordCount,sourceFile}], existingChapters, warnings}; confirm with POST /api/books/:id/import (JSON) {chapters:[{number,title,content,action:create|replace|skip}], actNumber} → {created, replaced, totalWordCount, chapterCount}. Avoid the legacy multipart POST /import — it pollutes manuscript/imported/ (export contamination) and mis-computes multi-file book stats.
- Infra matrix: Postgres + S3/MinIO + Redis required (S3 has NO fallback — every content op 500s without it; Redis absence HANGS agent-start rather than erroring). Neo4j optional (continuity scan → {flags:[], degraded:true}); Qdrant + OPENAI_API_KEY optional (vector memory silently skipped). Probe with GET /api/health/dependencies (public) → per-dependency {status, required, latencyMs}.
- Local auth for programmatic driving: .env has DEV_AUTH_BYPASS=true (user user_dev_bypass, onboarding complete) — plain HTTP calls to localhost:3000 need no auth header; E2E_TEST_SECRET also set for x-e2e-test-secret → user_test_e2e. STRIPE_SECRET_KEY is empty → plan gating fully open. NO LLM keys in .env — add one via the api-keys endpoint before any agent work.
- Local DB state: both prior deploy gates are applied (continuity_flags table exists; finding_replies.role and writer_memories.finding_id columns exist). Containers up: wmb-pub postgres+neo4j; redis/minio/qdrant currently served by the platform-new compose project on standard ports (6379/9000/6333).
- Conversational continuation: POST /api/books/:id/agent/:sessionId/message {message, pageContext?} → {ok:true} (only conversational workflows; survives server restart via ConversationTurn table). Approvals: POST .../approve {approvalId, decision: approve|reject|modify, message?}. Cancel: POST .../cancel → {ok:true} (DB status becomes 'failed').
- Usage: GET /api/usage → {total, byAgent, byModel, byBook, byKeySource, llmCosts, embeddingCosts, costDrift{driftPercentage, hasWarning@>20%}, priceDiscrepancies}; per-book GET /api/usage/books/:bookId. Pre-session: GET /api/books/:id/cost-estimate?workflowId=X → {blocked, costEstimate{min,max,formatted}, resolvedModel{registryId, tier, resolvedFrom}}.
- Canonical roadmap: docs/IMPROVEMENT-ROADMAP.md — Tier 1 wired, Tier 2 complete, shipped: 4.1, 4.2, 4.3-foundation, 4.4, 4.8; declared next: 4.6 power-user depth and/or Tier 3 moats; unshipped vision: 4.5 publication journey, 4.6, 4.7 language packs, Tier 3 collaboration/templates/evals.
- Unit tests: `npm run test:unit` → 25 files / 184 tests, verified passing this session in 1.7s; vitest node env, tests/unit only (vitest.config.ts include), coverage scoped to 6 money-path modules.
- E2E: `npm run test:e2e` → Playwright auto-starts `npm run dev` (webServer), global-setup seeds clerk_id 'user_test_e2e' via raw SQL (needs DATABASE_URL + docker-compose infra); auth bypass header x-e2e-test-secret must equal E2E_TEST_SECRET (default 'test-secret' in config), honored only when NODE_ENV !== production (src/middleware.ts:26-31, src/lib/auth.ts:54-56). 20 specs / ~125 cases; deployment-smoke.spec.ts excluded, runs via playwright.deployment.config.ts + `npm run test:deployment-smoke`. Projects: chromium + mobile-chromium (Pixel 7, mobile-*.spec only); firefox/webkit CI-only.
- DB deploy is `prisma db push` (no prisma/migrations dir — history drifted, per docs/production-readiness.md); guarded script: `npm run db:push:prod`. Pending push gates in schema.prisma: Book.archivedAt (:148), ContinuityFlag (:401), FindingReply (:423) + WriterMemory.findingId (:650, @@unique([userId, findingId, source])).
- QueryGraph tool (src/lib/agents/tools.ts:479) queryTypes: character-network | timeline | location-map | plot-threads | chapter-entities (needs chapterNumber) | consistency-checks; returns JSON.stringify of graph-queries results; error-swallowing (returns error string, never throws).
- src contains ZERO TODO/FIXME/HACK/XXX markers — all known debt lives in the roadmap's per-feature 'Deferred' lists and the superpowers specs' 'Explicitly deferred' sections.
- i18n: SUPPORTED_LANGUAGES = 14 codes (ui-strings.ts:325-340); full UIStrings translations exist only for en/sr/de/es/fr/ru/zh; getUIStrings(language) falls back en (:2365-2369). Book language flows to agents via prompt-assembler 'CRITICAL LANGUAGE REQUIREMENT' + LANGUAGE_NAMES map (:979) + per-language heading examples (:999). selectSkillsForAgent (src/lib/agents/skills/index.ts) has NO language parameter.
- Two commits post-date the last memory handoff: 76d4a66 'fix: OpenRouter routing — correct SDK baseURL (/api, not /api/v1) + drop dead LiteLLM detour' and 32bfc05 'feat: add Qwen 3.6 27B (OpenRouter) model family — 262K ctx, $0.285/$2.40 per 1M'.
- Competitive frame (roadmap Tier 3): Atticus + NovelCrafter = collaboration; Sudowrite = scaffolding; series depth + graph continuity = 'weak everywhere — open moat' (now WMB's shipped differentiator); 'best AI writing companion' per docs = 'the app understands what I'm trying to do, not checking my work' — write-first + conversational + ambient + live continuity (all shipped) + publication journey + transparency (not shipped).
- Untracked working-tree items: scripts/dev-seed-series.ts, scripts/dev-seed-shelf.ts (dev seeders relevant to validation), .playwright-mcp/. Branch main clean otherwise; no CLAUDE.md exists inside wmb-pub.
- Feature-count claims: definitions.ts AGENT_DEFINITIONS ~14-15 agents, workflows.ts ~30 id entries, model-registry ~37 model ids across 5 providers (Anthropic, OpenRouter, OpenAI, Gemini, Grok) — README's '14 agents / 27 workflows / 34+ models' is approximately accurate.