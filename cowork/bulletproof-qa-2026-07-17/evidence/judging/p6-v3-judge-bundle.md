# P6 "Owen" re-judge v3 — blind judge bundle (2026-07-27)

You are re-judging persona **P6 "Owen"** (voice-moat power writer, BYOK opus) after a fix+capture cycle targeting the v2 plateau.

## Baseline (prior verdict)
`evidence/judging/P6-REJUDGE-V2-AGGREGATE.md` — **6.5 unanimous**, floor = 6.5 plateau **D3/D4/D5**. Dim medians: D1 7.0 / D2 7.0 / D3 6.5 / D4 6.5 / D5 6.5 / D7 7.0 / D8 7.5 / D10 8.0 / D11 7.5. Raw verdicts: `p6-v2-raw-verdicts.md`.

## Since then (spot-check commits; view all PNGs yourself)
- **D-160/D-161/D-163 FIXED `6233c44`** + **D-173/D-174/D-175 FIXED `921cb90`**: ONE canonical 5-step setup accounting shared by wizard/banner/sidebar/overview (sidebar renders honest "✓ 2/5" — advancing to 5/5 was rejected as a lie); Start Writing! create-or-opens the lowest chapter IN THE EDITOR; 7-locale pluralization; wizard PATCH invalidates book-settings (no-reload flip); usage rows folded with auditable disclosure.
- **45-series capture** in `evidence/p6-owen-rejudge/UI-CAPTURE-2026-07-27.md` (+ 41c re-credit note in the 07-26 doc): 45a/g sidebar+overview truth incl. chapter-pipeline recommendation; **45b timed funnel: Start Writing! → editor 1.2s → first word 3.08s, zero POST /chapters (no dup)**; 45c **D-43 proven live on opus** (resolvedFrom book-role, $1.82 disclosed, usage row = session cost); 45d/f **D-172 discuss billing in pixels** (new Discuss agent row, bill-at-settle); 45e ghost SSE 141ms median; 45h no-reload chrome flip (0 doc loads, JS sentinel); 45i one folded Qwen row + slot disclosure.
- **D-162 SETTLED with evidence:** the "664 in / 4.0K out" row = 4 historic qwen36 reasoning-token rows (pre-`d51514c`), panel mapping faithful; residual = lifetime aggregate lacks date split (unnumbered).
- **Discuss surface now streams** (`eeb1fd8`, +38 tests) — P1 46-series evidence applies product-wide: ttft 19-36s honest wall (D-176 S3 registered), cadence 32ms, no-leak, cap 409 pre-stream, all-or-nothing abort.

## Disclosed and still open
- D-176 (S3) static wait line during provider reasoning phase (applies to Owen's discuss too); D-177/D-178 (S4).
- From v2/earlier: D-49 (fingerprint rationale quote fabrication) and D-42 NOT re-probed; D-139-family FAB occlusion recurrence; line-edit spend attributed to "Writing Coach" row (unnumbered); 45c per-frame cadence instrumentation failed (harness, disclosed); "Loading chapter…" JIT frame in 45h (dev-server artifact, disclosed).
- Identity via e2e headers (not Clerk login); setupComplete reset lever used again for 45b/45h (PATCH documented); wizard walked Skip-path (import/style legs still unexercised in UI).

## Your job
Score every baseline dim 1-10 (0.5 steps) AS EVIDENCED NOW. Overall = floor-bound MIN. Name floor dim + driver, list new defects in the pixels the docs missed (next free D-179 — do not renumber), and the single next action that most raises the floor.
