# P1 Maya — Judge verdict: TRUST + MANUSCRIPT INTELLIGENCE lens (disqualifier hunter) (blind, 2026-07-18)

> Verbatim final report of blind judge (bundle: p1-maya + competitive-teardown.md). Saved unedited by team-lead.

All 26 tool-level quote instances in the SSE streams are also byte-verbatim. My earlier "missing words" impression was my own display-compression artifact — the same console-display failure mode the executor documented and retracted. Verification complete; issuing the verdict.

---

# BLIND JUDGE VERDICT — Panel P1 (Maya), lens: TRUST + MANUSCRIPT INTELLIGENCE (disqualifier hunter)

Bundle: `D:\Projects\wmb-pub\cowork\bulletproof-qa-2026-07-17\evidence\p1-maya\` + `evidence\competitive-teardown.md`. All scores re-derived from raw evidence; executor-suggested verdicts ignored.

**Quote-verification statement (hard rule discharged):** I byte-verified EVERY quoted `originalText`, `anchorQuote`, and `alternatives[].originalText` in every findings-bearing file (7 API captures + 3 dismiss traces) AND every CreateFinding `toolInput` inside all three dev-edit SSE streams against the in-bundle manuscript captures (`api-traces/d8-rerun-chapter-content-before.json` and `d13-reverify-chapter-content-before.json`, byte-identical to each other, sha256 `e2e0f62e...` matching the journey log's recorded hash). Result: **162 quote instances (26 unique strings), zero misquotes** — every anchor is a byte-exact substring of the manuscript. The two claimed lineage hashes (`877f2421...` 313 chars; `f01da3a4...` 300 chars) reproduce exactly. No D8 misquote cap applies. No continuity flags exist in the bundle (no continuity-net run), so no false-positive cap applies. No voice-flattening found: the dev-editor's rewrites conspicuously preserve the manuscript's signature devices (its own rationale for `d0f79766` defends the fragments as "signature rhythm," and its Option A proposes ending ON the fragments — the writer's exact stated aesthetic). Worker-proof (`worker-proof.txt`) shows exactly one leaf worker runtime (PID 37060), full wrapper chain resolved, re-verified unchanged after the mid-session app-server restart — agent measurements are NOT void.

## 1. Per-dimension scores

- **D1 Functionality — 6.0.** The persona's headline journey (discuss → memory → honored) was 100% non-functional at first contact — 3/3 discuss turns returned HTTP 200 with empty `assistantMessage` (`transcripts/discuss-turn{1-retry,2,3}.json`) — and reaching a passing state required two mid-campaign product fixes (D-04, D-13). Post-fix, the loop completes (`d8-rerun-turn{1,2,3}.json`, `d8-rerun-memory-after-dismiss.json`, `d13-reverify-findings-after.json`); edge cases, return-visit, and tier probes pass. Works now, but only because the campaign patched it live.
- **D2 Reliability & data safety — 6.5.** Strong positives: no partial write after malformed JSON (`d01-repro-control-get.json`), background agent jobs survive client disconnects with full SSE replay (five client-timeout stream captures for session `eeef608c`, later re-attach returns full history), version history real, worker singleton held through an app-server restart. Docked hard for the original silent empty-200 — the textbook dishonest failure state — and for the replacement honest-502 path being live-untriggered (unit-test only, executor admits this). Malformed JSON still 500s (D-01).
- **D3 Usability — 6.0 (API-surface only).** Excellent plain-language copy on gates: turn-cap 409 ("You've discussed this finding thoroughly (3 exchanges). Ready to make a decision?"), tier 403 with `upgradeToTier`, quota 403, invalid-key 400 (`step2-step4-batch.json`). But malformed input yields a generic 500 "Failed to update book", and pre-fix discuss gave the writer zero signal that nothing happened.
- **D3b Ergonomy — NO-EVIDENCE.** Raw HTTP throughout; no click paths, keyboard, or UI dead-end evidence.
- **D4 Onboarding — 5.5 (partial evidence).** Setup pipeline (capture-style → story bible → architecture) completed and produced a genuinely impressive voice fingerprint (`fingerprint.json`), but `build-architecture-sse-raw.json` shows a 360s client timeout with only 79 events before re-attach recovered 2534 — a fresh user watching that stream would conclude it hung. No time-to-first-word evidence proper.
- **D5 Performance feel — 6.5 (dev-server + BYOK labeled).** Queue honesty is real: `queued:true`+jobId, streaming progress, in-stream `cost_update` events with live USD and $5 budget visible (dev-edit-3 events 2/8/12). But discuss turns run 25–49s (one exceeded a 60s client timeout), dev-edits 147–437s, and long streams routinely outlive client patience.
- **D6 Look & feel — NO-EVIDENCE.** No UI captures of any kind.
- **D7 Trust & safety — 7.0.** Every probed gate held with zero blast radius: series-create 403 then immediate 200 on owned book; book-quota 403 fires before body parsing; invalid BYOK key rejected cleanly with no key echo or internal detail anywhere in the bundle; per-run budget cap visible in-stream. Docked for: D-01 error hygiene (500 on malformed input, 3rd route confirmed), raw internal `TypeError` strings crossing the tool boundary (D-33/D-34 class), and two unexplained "Rita QA" seeded findings sitting inside Maya's own book (see Defects, J-3).
- **D8 Manuscript intelligence — 6.5.** Anchoring fidelity is flawless — 162/162 byte-verbatim, grounding scores honest, three labeled alternatives per finding, no flattening; discuss (post-fix) genuinely adapts to stated intent and emits accurate REMEMBER constraints (`d8-rerun-thread-check-after-timeout.json`). But the dimension's memory clause failed live: dev-edit-3 re-raised the dismissed, memory-backed critique (D-13, byte-identical originalText, verified), the deterministic fix has never fired live (executor admits: 0 suppression events in dev-edit-4), and — judge-found — the editor separately duplicated a still-PENDING finding three times across three runs (J-1 below), so "tell it once" fails in a second, unfixed direction. No cap triggered, but these dominate the score.
- **D9 Retention / habit — 7.0.** Stats math verified exact: dailyCounts 380+324=704 equals actual chapter word count, streak/bestStreak/weeklyAvg all recompute correctly from real `document_versions` deltas (`return-visit-stats.json`); daily plan reflects the real chapter status (`dev_edited` confirmed in `d8-rerun-chapters-list.json`). Single probe, so not higher.
- **D10 Delight — 6.5.** The discuss replies show real editorial intelligence ("The fragments land precisely because they refuse to explain themselves") and the dev-editor's rationale internalizes the writer's craft vocabulary. Undermined by the writer seeing the identical critique three times.
- **D11 Competitive edge — 6.0.** The teardown's claimed moat (quote-anchored findings + discuss + WriterMemory — "learns from your rejections") is exactly what this bundle stress-tested, and the anchoring half is genuinely best-in-category (nobody else byte-anchors edits). But the learning half — the actual switching pitch — failed live for this persona's model class until patched mid-campaign, is once-verified, and duplicates accumulate. For a debut Indie BYOK writer (teardown Gap #4: "BYOK is a wall for beginners"; Muse/zero-setup is the incumbent draw), the evidence supports "promising, not yet leave-your-tool-and-pay."

## 2. Defects I found (missed or misrepresented by the executor)

- **J-1 (S2-borderline-S3, MISREPRESENTED): duplicate PENDING findings accumulate; the "genuinely-new finding" is a third copy.** The show-tell critique of paragraph 6 was created three times with byte-identical 300-char `originalText` (sha `f01da3a4`), same category, same paragraph, same severity: `73b2781c` (dev-edit-2, 07-17T22:38), `5c20c0e1` (dev-edit-3, 07-18T00:13), `f1b35402` (dev-edit-4, 07-18T11:36) — all simultaneously "pending" in `api-traces/d13-reverify-findings-after.json`. The journey log's d13rv-verdict-check calls `f1b35402` "a DIFFERENT passage ... Zero re-raises ... 1 genuinely-new finding, so no over-suppression" — different from the *dismissed* lineage, yes, but it is a byte-identical re-raise of a pending finding, so "genuinely-new" is false and the no-over-suppression proof is built on a duplicate. The D-13 fix only matches DISMISSED findings; the executor's own dedup analysis (reworded descriptions → different contentHash → dedup never fires) applies equally here and was never connected. A real writer gets the same critique stacked three deep after three editor runs.
- **J-2 (evidence integrity): D-33 frequency understated and its first occurrences undisclosed.** The identical `Cannot read properties of undefined (reading 'normalize')` CreateFinding crash occurred TWICE in dev-edit-3 (`d8-rerun-dev-edit-3-sse-raw.json` events 4 and 6 — the second crashed attempt being the D-13 re-raise itself) before the dev-edit-4 occurrence the defect entry credits as the discovery. That is ≥3 occurrences in 2 consecutive runs — a per-run event burning paid tokens (~$0.018 already spent by event 2), not a one-off curiosity — yet the journey log records dev-edit-3's stream as a clean PASS row.
- **J-3 (unexplained cross-persona artifact): two "Rita QA" findings inside Maya's book.** `findings.json` rows `58d89462` / `2220ab58` ("Rita QA rate-limit target...", agentType "agent", 07-17T15:25–27) sit in Maya's bookId and are counted in the "7 findings" baseline. If harness-seeded, fine — but the bundle never says so; if created via another persona's session, it would be an ownership breach. A trust-lens executor should have explained rows bearing another persona's name in this persona's book.
- **J-4 (S4): the voice fingerprint misstates manuscript structure.** `fingerprint.json` presents "She would wade." as a stand-alone-paragraph fragment signature; in the manuscript it is mid-paragraph (para 2). Also "Analysis Date: 2024" (wrong year) and "7 paragraphs" vs findings addressing paragraphNumber 8. Cosmetic, but this artifact is injected into every agent prompt.
- **J-5 (uncaptured state change): chapter growth 570→704 words** between dev-edit-1 (finding `511cc83b` says "~570 words", 16:02) and the later captures is asserted in the journey-log header but no content-write trace exists in the bundle; the mutation event that changed the writer's manuscript is unevidenced. (All quote checks used the in-bundle 704-word captures, so anchor verification is unaffected.)
- **J-6 (minor data hygiene): `rejectedAt` set on a writer-dismissed finding.** `transcripts/finding-dismiss.json`: status "dismissed", `dismissReason` populated, yet `rejectedAt: 2026-07-17T22:32:48.959Z` with `rejectionReason: null` — the rejection and dismissal lifecycles bleed into each other.
- **J-7 (design observation): last-constraint-wins silently discards intent.** Turn-2's stored-candidate constraint ("Flag and cut any strong final image...") and turn-3's persisted one ("Leave strong final images... treat as intentional") are contradictory; only turn 3 persisted (`d8-rerun-memory-after-dismiss.json`). The writer's manuscript-wide "flag this elsewhere" request from turn 2 evaporated. Not noted by the executor.

## 3. "Suspiciously clean" analysis

This bundle is unusually honest in the failure-preservation direction: it keeps the original all-empty discuss run, the ENV-01 blocker writeup, five timed-out stream captures, two self-retracted false positives with the correction mechanism shown, and explicit "not live-triggered" caveats on both fix verifications. That discipline is real and rare. The curation risk is in the **fix narrative**: both "FIXED-VERIFIED" stamps are softer than they read — the D-04 502 path and the D-13 suppression gate have each fired only in unit tests, never live; the D-13 outcome proof leans on a "genuinely-new finding" that is actually a third duplicate (J-1); and dev-edit-3's two tool crashes were left out of the run's PASS row (J-2). What I expected and did not find: any UI evidence, any line-edit run (the D8 clause "line-edit preserves voice" is untested for a prose-focused persona), any continuity-net scan, any export exercise, the 570→704 manuscript mutation, and any second BYOK model (the entire agent story rests on one model config — the same config that shipped completely broken, proving this path had never been smoke-tested before the campaign). Verdicts above discount for all of this.

## 4. One-line overall impression

Forensically honest QA of a genuinely differentiated editorial engine whose anchoring is flawless (162/162 byte-verbatim) but whose signature promise — "tell it once, it remembers" — is evidenced as freshly patched, once-verified, and still leaking duplicates the executor mislabeled as new; a 6-something product with an 8-something moat under construction.
