p6 | FUNCTIONALITY + RELIABILITY | headline=6.5 | floors=D3 | D1=7.5 D10=8 D11=8 D2=8 D3=6.5 D3b=NO-EVIDENCE D4=NO-EVIDENCE D5=7 D6=NO-EVIDENCE D7=7 D8=8 D9=NO-EVIDENCE
p6 | UX / EXPERIENCE | headline=6 | floors=D4,D5 | D1=7 D10=7.5 D11=7 D2=8 D3=6.5 D3b=NO-EVIDENCE D4=6 D5=6 D6=NO-EVIDENCE D7=6.5 D8=7.5 D9=NO-EVIDENCE
p6 | TRUST + MANUSCRIPT INTELLIGENCE | headline=6.5 | floors=D7,D5 | D1=7.5 D10=8 D11=7.5 D2=7 D3=7 D3b=NO-EVIDENCE D4=NO-EVIDENCE D5=6.5 D6=NO-EVIDENCE D7=6.5 D8=7.5 D9=NO-EVIDENCE
p8 | FUNCTIONALITY + RELIABILITY | headline=6.5 | floors=D5 | D1=8 D10=NO-EVIDENCE D11=NO-EVIDENCE D2=8 D3=7.5 D3b=NO-EVIDENCE D4=7 D5=6.5 D6=NO-EVIDENCE D7=8.5 D8=NO-EVIDENCE D9=7
p8 | UX / EXPERIENCE | headline=7 | floors=D3 | D1=7.5 D10=NO-EVIDENCE D11=NO-EVIDENCE D2=7.5 D3=7 D3b=NO-EVIDENCE D4=NO-EVIDENCE D5=NO-EVIDENCE D6=NO-EVIDENCE D7=8 D8=NO-EVIDENCE D9=NO-EVIDENCE
p8 | TRUST + MANUSCRIPT INTELLIGENCE | headline=7.5 | floors=D3 | D1=8 D10=NO-EVIDENCE D11=NO-EVIDENCE D2=8 D3=7.5 D3b=NO-EVIDENCE D4=NO-EVIDENCE D5=NO-EVIDENCE D6=NO-EVIDENCE D7=8.5 D8=NO-EVIDENCE D9=NO-EVIDENCE
p1 | FUNCTIONALITY + RELIABILITY | headline=6.5 | floors=D5 | D1=8 D10=7.5 D11=7.5 D2=8 D3=7 D3b=NO-EVIDENCE D4=NO-EVIDENCE D5=6.5 D6=NO-EVIDENCE D7=7.5 D8=8 D9=7.5
p1 | UX / EXPERIENCE | headline=6 | floors=D3b | D1=8 D10=7 D11=7 D2=8 D3=6.5 D3b=6 D4=NO-EVIDENCE D5=6.5 D6=NO-EVIDENCE D7=7.5 D8=7.5 D9=7.5
p1 | TRUST + MANUSCRIPT INTELLIGENCE | headline=6.5 | floors=D3,D5 | D1=7.5 D10=7 D11=7 D2=8 D3=6.5 D3b=NO-EVIDENCE D4=NO-EVIDENCE D5=6.5 D6=NO-EVIDENCE D7=7.5 D8=7.5 D9=7
p7 | FUNCTIONALITY + RELIABILITY | headline=7 | floors=D3,D10 | D1=8.5 D10=7 D11=7.5 D2=9 D3=7 D3b=NO-EVIDENCE D4=NO-EVIDENCE D5=7.5 D6=NO-EVIDENCE D7=7.5 D8=NO-EVIDENCE D9=NO-EVIDENCE
p7 | UX / EXPERIENCE | headline=7 | floors=D3 | D1=9 D10=7.5 D11=7.5 D2=9 D3=7 D3b=NO-EVIDENCE D4=NO-EVIDENCE D5=7.5 D6=NO-EVIDENCE D7=8 D8=NO-EVIDENCE D9=NO-EVIDENCE
p7 | TRUST + MANUSCRIPT INTELLIGENCE | headline=7 | floors=D3,D10 | D1=8.5 D10=7 D11=7.5 D2=9 D3=7 D3b=NO-EVIDENCE D4=NO-EVIDENCE D5=7.5 D6=NO-EVIDENCE D7=8 D8=NO-EVIDENCE D9=NO-EVIDENCE


# Judge-surfaced defects + suspiciously-clean answers (raw)

### p6 / FUNCTIONALITY + RELIABILITY
SUSPICIOUSLY-CLEAN: No â€” this bundle does not look suspiciously clean. It records live open defects against itself (D-43 silent editor-model misroute with qwen-priced billing proof, D-49 fabricated fingerprint quotations independently re-verifiable against the 24,969-char raw fingerprint embedded in p1d, D-42 framework HTML 404), captures a live malformed-finding REJECTED corrective loop in the raw SSE, files two genuinely new adversarial defects (N-1 overlap dead-end, N-2 memory near-duplicates), honestly downgrades its own script's inflated recall metric from 4/6 to 3/6, and explicitly declares D-36 (outage fake-success) and D-40 (fingerprint regen) as NOT-TESTABLE rather than claiming them fixed. Failure evidence I would expect but is missing: (1) no in-bundle copy of the ch5 manuscript, so 2 of 3 anchor byte-verifications depend on executor-computed booleans; (2) the post-discuss full finding row (originalText) was not captured â€” precisely where the freshly-fixed D-41b path could hide a destructive span mismatch; (3) the D-43 run's SSE was saved only as a summary; (4) zero UI/screenshot evidence â€” the entire bundle is API-level, so D3b/D4/D6/D9 are untestable and real-UI failure modes (rendering of REMEMBER markup, rejected-row clutter, diff display of the 3-sentence span) are unprobed; (5) no SSE-disconnect/reconnect, concurrency, or provider-outage probes; (6) the 3/6 recall was accepted without probing styleStrictness settings. The honesty-surface all-pass pattern (7 baseline defects CLOSED) is plausible given a landed fix-wave and each closure has a consistent raw trace, but the cluster of gaps concentrates exactly around the newest fix (D-41b persistence), which is where I found the one plausible undisclosed hazard.
- [S3 (plausible, unconfirmed)] D-41b closed without span-coherence check: the discuss compromise for b48e321f is sentence-scoped (targets only the sunset sentence) but was persisted onto newText of a finding whose originalText spans 3 sentences including the V1 and-stack device and the tin plot beat; post-discuss originalText was never captured, so if unchanged, applying the finding as persisted would silently delete ~2 sentences of the writer's prose. The finding was deliberately left pending in this state.
- [S4 (evidence completeness)] Bundle contains no copy of the ch5 manuscript (planted-probe file lives outside the bundle at ../p6-owen/manuscripts/), so independent byte-verification of the sunset and dangler anchors rests on executor-computed booleans; only acaf7362's anchor is independently proven by the server-side exact-span apply (200 + onlyExpectedDelta:true).
- [S4 (evidence completeness)] D-43 probe saved only a summarized SSE transcript (3 cost-update model names), not the raw event stream as done for ch5; the journey-log's 'every cost_update streamed qwen' claim is supported by the summary + billed usage row but is not fully raw-verifiable.
- [S4] Raw DB-stored assistant reply for the dangler discuss contains internal control markup ('<<<REMEMBER category="preference">>>...<<<END>>>') in the message content; if the UI renders stored replies verbatim, writers see internal plumbing. Unflagged by executor; UI side unverified.
- [S4] ch3 LINE_EDIT_REPORT document has title:null (all other chapter reports are titled) â€” minor persisted-data-quality wart visible in report-docs-meta, unflagged.
- [info] Journey-log 'Positive notes' cites optimistic locking / D-47 stampless-overwriteâ†’409 as if attested, but no 409/concurrency probe exists in this bundle â€” that claim is carried over from baseline, not re-proven this run.
- [info] p1b's automated metrics ('plantRecall 4/6', 'devicePrecision 5/6') are span-touch double-counts (E1's double-under survives verbatim in the critical finding's newText; V1 is 'flagged' only by span inclusion but preserved). The journey-log corrected these downward to 3/6 and 6/6 â€” an under-claim, not an inflation, but the raw trace metric is misleading if read alone.
- [info] Journey-log timezone note is backwards: it says trace times are server-local and DB timestamps are UTC, but the trace ISO times (runStart 11:30:39Z) are true UTC (=13:30 local, inside the worker bracket) while the raw-pg reads (09:30:41Z) are the skewed ones (UTCâˆ’2h from naive-column parsing). Timeline is coherent once reconciled, but a reader trusting the note would misplace the runs relative to the 13:23 worker capture.

### p6 / UX / EXPERIENCE
SUSPICIOUSLY-CLEAN: No â€” this bundle is notably NOT suspiciously clean, and that raises its credibility. It contains raw negative evidence throughout: a mid-run REJECTED malformed CreateFinding (null anchor) captured in the SSE stream with its corrective recovery; two still-open defects (D-43 silent editor-model misroute, D-49 fabricated fingerprint quotation) proved live against this run's own output; a framework HTML 404 (D-42) recorded raw; the executor's own query bug (p3 empty writer-memories via clerk-id filter) left in the trace with the honest p3b correction rather than silently re-run; and explicit NOT-TESTED disclosures (D-36 outage fake-success path requires a real provider failure; D-40 fingerprint not regenerated). Failure evidence I would still expect but is missing: (1) zero UI evidence â€” no screenshots at all, so keyboard ergonomics, themes/contrast/empty-loading states, and retention surfaces are unassessable in an 'experience' bundle; (2) no SSE mid-stream disconnect/reconnect probe â€” especially important since D-42 means a disconnected client has NO recovery path, and that combination was asserted but never demonstrated end-to-end; (3) no concurrent-edit probe on apply (the optimistic-locking 409 is carried from baseline, not re-shown); (4) no negative-path probe on discuss (e.g., discuss on an applied/dismissed finding, or provider timeout mid-discuss â€” relevant given the observed 157s blocking turn); (5) all voice-quality verdicts are conditional on a single model (qwen3.6-27b) â€” no cross-model probe of the moat. Also note the pre-registered manuscript and device registry live outside the bundle directory; anchors were verified via in-bundle script byte-checks plus the exact-span apply corroboration, which I accepted, but a bundle-internal manuscript copy would have made byte-verification fully self-contained.
- [low] Executor over-claimed the 'milder' D-49 instance: the phrase 'almost entirely avoids abstract psychological vocabulary' IS present in the fingerprint doc as 'almost entirely **avoids abstract psychological vocabulary**' â€” the verbatim check failed only on markdown bold markers, so this is NOT a fabricated quotation (the primary instance 'clipped, procedural, emotionally controlled' is genuinely absent and D-49 still stands on that).
- [medium] Raw memory-extraction protocol markup ('<<<REMEMBER category="preference">>>...<<<END>>>') is persisted verbatim inside the assistant's stored finding_replies content; if the UI renders discuss history from these rows the writer sees raw protocol markup. Not flagged by the executor (API response strips it into suggestedConstraint, but the DB row retains it).
- [medium] Discuss is a blocking, non-streaming POST: the hold-ground turn took 157.2s synchronous wall time (discuss-crit-holdground elapsed 157.246) with zero interim feedback â€” a 2.5-minute dead spinner for a writer mid-conversation. Executor recorded the elapsed time but did not flag it as a UX defect; the adapt turn (22.5s) shows high variance too.
- [low] The p1b trace's machine metrics are misleading on their face: 'plantRecall 4/6' and 'devicePrecision 5/6 survive' are span-subsumption artifacts (b48e321f's 3-sentence span textually contains E1 and V1 without addressing/removing either). The journey-log narrative correctly re-derives 3/6 recall and 6/6 devices, but a reader trusting the raw trace metric would over-credit recall.
- [low] Chapter 3's LINE_EDIT_REPORT document has title: null (all other chapters' reports are properly titled), and the p5 ch3 run wrote to that same doc without fixing it â€” a user-visible document-list polish wart not flagged by the executor.
- [medium] The persisted user-visible FINGERPRINT document still contains glitch tokens ('nicht by training', 'die-sel', 'asnaczyniÄ…', 'semdp-wisdom', 'Parallelismæ— æ„', 'monoologue') â€” executor disclosed D-40 as not re-tested, but did not weigh that a craft-serious writer opening their own style fingerprint today reads corrupted text; this remains a live experience defect regardless of regeneration status.

### p6 / TRUST + MANUSCRIPT INTELLIGENCE
SUSPICIOUSLY-CLEAN: Not suspiciously clean â€” the bundle contains genuine failure evidence recorded raw: a live malformed CreateFinding REJECTED and recovered (D-33/34 corrective loop), two reproduced-open defects (D-43 silent model misroute with billing proof, D-49 fabricated fingerprint quotation), a framework-HTML 404 on session-status (D-42), the executor's own p3 query bug honestly preserved and corrected in p3b, and a painful 157s blocking discuss POST left in the trace. Worker-proof is exemplary (one leaf PID bracketed pre/post with full node.exe attribution). BUT meaningful failure evidence IS missing: (a) zero UI evidence â€” no screenshots at all, so D3b/D4/D6/D9 are unassessable and 'usability' claims are API-only; (b) no ownership/cross-tenant/tier-gate negative probes this cycle despite the trust lens; (c) no induced provider-failure or worker-kill mid-run test â€” D-36 fake-success declared not-testable, yet killing the single worker mid-session was feasible and would have probed the honest-failure path; (d) the post-discuss state of b48e321f's originalText/anchor was never captured â€” exactly where the fresh D-41b fix could hide a destructive regression, and the finding was left pending/armed; (e) traces store matchesPlanted booleans instead of the fetched chapter bytes, so anchor verification required the referenced pre-registered probe file rather than being self-contained; (f) the ch4 adversarial chapter (comma-splice/fragment respect) and a fresh fingerprint regeneration (D-40) were not re-run. The positive results that ARE here survived my independent byte-verification (0/3 misquotes, V1 preserved verbatim, report quotes and stylometric counts all true against the manuscript), so I judge the bundle honest but narrower than its confident tone suggests.
- [high (plausible, unverified â€” post-discuss originalText not captured)] D-41b closure under-verified and plausibly introduces a destructive-apply trap: post-discuss, b48e321f.newText was overwritten with the 2-sentence compromise ('Light shifted to red behind the island, then gold. Visibility held good.') but originalText (a 3-sentence span containing the palpable sentence, the V1 and-stack device, and the plot-critical tin discovery) was never re-captured. If the route did not narrow the span, applying this still-pending critical finding against the restored chapter (where the span matches again) would silently delete two sentences of prose the discussion never proposed removing â€” including a registered signature device. The trap is armed in the bundle's final state; the 422 guard only covers BLANK newText.
- [medium] Live STYLE FINGERPRINT doc â€” the voice moat's core writer-facing artifact, consumed as agent input on every line-edit â€” contains at least 6 glitch corruption tokens ('nicht by training', 'die-sel', 'asnaczyniÄ… of emotional closure', 'semdp-wisdom', 'Parallelismæ— æ„ stacked', 'internal-monoologue'). Executor deferred this as stored-artifact D-40 but did not flag that the corrupted doc is still being served live and fed to the editor each run.
- [medium (evidence-completeness gap)] No ownership/cross-user or tier-gate negative probes anywhere in this rejudge bundle â€” the trust surface most central to this lens was not re-tested this cycle, and the executor did not note the omission as a limitation.
- [low] Executor over-claim: journey-log and defects.md state the persisted ch5 LINE_EDIT_REPORT 'AND' the finding rationale both quote the fabricated fingerprint phrase 'clipped, procedural, emotionally controlled'. Grep of p1c shows the phrase appears ONLY in b48e321f's rationale; the report paraphrases ('procedural-maritime-grounded') without quotation marks. D-49 is real but its blast radius is overstated.
- [low] Executor mischaracterized the 'milder D-49 instance': the fingerprint doc DOES contain 'almost entirely **avoids abstract psychological vocabulary**' â€” acaf7362's quote is faithful modulo markdown bold markers, not 'adds words inside the quotes'. Only the byte-compare fails; the words are present in sequence.
- [low] The ch5 report actively PRAISES paragraph 1 as 'exemplary' opening precision while quoting around planted error E3 ('I saw that the rope had frayed...' â€” the report quotes 'rope had frayed where it crossed the iron ring', eliding the filter-word construction it should have flagged). Praising a planted error's sentence is a sharper intelligence miss than a plain recall miss; executor logged E3 only as 'missed'.
- [low] p1b's machine-computed metrics block ('plantRecall 4/6', 'devicePrecision 5/6 survive') contradicts the narrative (3/6 flagged, 6/6 survive) because it counts span-touches, not flags/removals. The narrative re-derivation is correct per the pre-registered survival rule (I verified V1 preserved verbatim in newText), but the raw trace as written could mislead a reader in either direction.
- [low (UI impact unverified)] Raw '<<<REMEMBER category="preference">>>...<<<END>>>' control markup is persisted verbatim inside finding_replies.content (the API response stripped it, but the DB row retains it). If any UI renders reply history from the DB, internal prompt scaffolding leaks to the writer. Not probed by the executor.

### p8 / FUNCTIONALITY + RELIABILITY
SUSPICIOUSLY-CLEAN: Yes, the bundle reads clean at first glance â€” 34/34 probes pass and the journey-log claims 'zero cross-tenant leaks, zero tier bypasses, zero raw-500, zero key leaks, zero silent-allow.' But this is a security-NEGATIVE-probe bundle, so all-green is the EXPECTED shape when fences hold, not automatically suspicious. Several signals argue against fabrication or cherry-picking: (a) the traces carry differentiated real data â€” p1 usage {totalTokens:700149, cost:0.354, sessions:8} vs p8 {0,0,0}, real varying dependency latencies (postgres 98/schema 209/redis 93/worker 62ms), real word counts in D8 analytics, and B10/C2 victim-intact sanity reads proving the DELETE/DELETE-lens attacks were true no-ops; (b) the executor did NOT hide the one imperfect response (K2 405 empty-body is recorded as N-1); (c) the limitations section honestly marks proration/D-06-second-leg/discuss-cap/agent-latency as NOT-TESTED rather than asserting them. What failure evidence I would still expect and is MISSING: (1) at least one probe proving the harness can DETECT a failure in the current build â€” every closed defect is asserted 'was 500/401/200' historically, but no live-failing case in THIS capture demonstrates the detector still fires (the K2 nit partially covers this); (2) a negative control rejecting a wrong/absent E2E secret (401) to prove the backdoor is gated; (3) any exercise of the real production auth path â€” all fences are tested through a dev impersonation header; (4) a positive own-book export-integrity/no-truncation test. Net: credibly clean rather than suspiciously clean, but its confidence is bounded by an untested production auth boundary and a thin/absent perf-feel (D5) surface, which is why D5 floors the persona grade despite the 8-8.5 trust/reliability posture.
- [medium] Every fence/gate/ownership result is validated only through the dev E2E backdoor (x-e2e-test-secret + x-e2e-clerk-id, which short-circuits real Clerk auth). The bundle proves app-layer fences but never exercises the production authentication boundary and never verifies the E2E backdoor is disabled in prod. All no-bypass claims are therefore conditional on that backdoor being unreachable in production â€” a scope caveat the executor under-discloses.
- [low] K4 (unrecognized clerkId 'user_hackerman_admin' + valid shared secret) returns 200 with [] rather than 401. The executor's claim that it 'forces the fixed fallback user' is an inference not directly evidenced by the trace (an empty list is also consistent with a fallback user who happens to own no books). Regardless, silently accepting an impersonation attempt with a 200 is weaker honesty than a 401 reject, and it is under-flagged (recorded as a clean pass, not a hygiene nit).
- [low] The lens sub-claim 'export never truncates' is not positively tested. B9 only exercises the cross-tenant export fence (404). No trace confirms a legitimate own-book export completes with full, untruncated content â€” an unverified D2 coverage gap not called out in the limitations section.
- [low] No negative control proves the backdoor is actually gated: there is no probe showing a request with a WRONG or ABSENT x-e2e-test-secret is rejected (401). Every probe supplies the valid secret, so the evidence shows the fences hold for authenticated callers but never demonstrates the secret itself is load-bearing.
- [low] K2 405-with-empty-body was recorded (N-1) but is the one response in the sweep that breaks the standard {error} envelope the campaign has been closing elsewhere (D-15). Correctly rated low, but it is a genuine reliability/honesty inconsistency, not purely cosmetic â€” monitoring/clients get a bare 405 with nothing actionable.

### p8 / UX / EXPERIENCE
SUSPICIOUSLY-CLEAN: Two-part answer. (1) On its own security/fence terms this bundle is very clean (0 cross-tenant leaks, 0 tier bypasses, 0 raw-500, 0 key leaks), but it is NOT suspiciously clean in a dishonest way: it surfaces one honest negative (K2 405 empty-body) rather than hiding it, every headline claim verifies byte-for-byte against the cited raw trace (A1/D2/D4/E5/B1/H1/F1/G1/I1/K4/D8/B10/C1/E4/B9 all checked), worker-proof.txt is thorough (exactly one worker runtime, PID 61892), and scope-not-exercised is disclosed up front (proration, discuss cap, agent latency). (2) From MY UX/EXPERIENCE lens it is suspiciously clean of EXPERIENCE evidence â€” that is the real gap. What I would expect and do NOT see: any screenshot of what a user actually sees when they hit the 403 paywall or a 404/500 (does the good JSON copy render as a friendly upgrade modal or a raw error page? unknown); an onboarding/time-to-first-word capture; empty/loading state and theme/contrast/locale evidence; a real export producing intact content (B9 only proves the ownership fence, never that export doesn't truncate); and any incumbent comparison for D11. Because the low-floor UX areas (onboarding friction, visual polish, competitive edge) are simply never probed here, I cannot award high experience marks â€” I can only certify that the error-legibility slice (D3) is good, which floors the lens at 7.0. The bundle is honest about being a fence/gate/money sweep; it just is not an experience artifact, so most UX dimensions are correctly NO-EVIDENCE rather than fabricated.
- [medium] Bundle contains ZERO UI/screenshot/click-path evidence, yet is submitted for an experience judgment. From the UX lens, onboarding (D4), ergonomy (D3b), look-and-feel (D6), and competitive edge (D11) are entirely unevidenced and cannot be scored; only API error-legibility (D3) is gradeable. Not misrepresented by the executor, but a hard coverage ceiling for this lens.
- [low] D-15 wiki 400s are enveloped and honest but expose raw developer-oriented Zod messages (e.g. 'Invalid option: expected one of "character"|"location"|...'); defects.md/journey-log frame these as user-'readable field errors', slightly overstating end-user legibility. An end user seeing raw enum dumps is not the same UX quality as the polished gate/billing copy.
- [low] D2 'export never truncates' is unproven: B9 only exercises the ownership 404 fence on /export; no real export content or integrity is captured anywhere in the bundle. The reliability claim rests on the fence, not on actual export behavior.
- [low] All ownership/tier fences run through the x-e2e-test-secret + x-e2e-clerk-id harness bypass, not the real Clerk auth boundary. K4 honestly discloses the prefix-guard, but 'no-bypass' is proven only for the harness identity model; the production auth boundary is untested.
- [low] N-1: GET /api/series/{id}/books returns 405 with a zero-length body (K2) â€” the one non-enveloped response in the whole sweep, touching both D3 and D6 error-state hygiene. Correctly self-reported by the executor as low/cosmetic; not missed, but it is the same honesty/consistency class the campaign closed elsewhere (D-15) and should be tracked, not left open.

### p8 / TRUST + MANUSCRIPT INTELLIGENCE
SUSPICIOUSLY-CLEAN: Yes â€” 33/34 probes land exactly as desired with only one minor nit (K2 405 empty-body), which is unusually tidy for an adversarial sweep. But the cleanliness is largely CREDIBLE, not fabricated: (a) these are deterministic gate/fence/validation checks re-run against an already-fixed codebase, so passing is expected; (b) the bundle byte-verifies victim-intact via real positive controls (B10 shows p1's book with wordCount 704 and chapter present after the DELETE attack; C2 shows the victim lens surviving; D6/D8/F5a return real created/analytics payloads), which proves the 404s are genuine no-row results, not masked 500s; (c) it honestly discloses coverage gaps (W6 proration, D-06 second leg, discuss-cap all marked NOT-TESTED) and self-reports the one nit. MISSING failure evidence I would expect: (1) no in-bundle before/after RED repro â€” it asserts 'was 500 / was 401 / was 200 double-bill' but includes no captured trace of the OLD broken behavior, so the baseline labels are trusted, not shown; (2) no production-auth-boundary probes â€” everything rides the x-e2e-test-secret shim, so token forgery, expired/missing session, and 401 paths are untested; (3) narrow malformed-input coverage for the 'architectural ~46-route' D-01 claim (only ~4 routes probed; the rest inferred from source, partly corroborated by A3); (4) no wide 500-hunt (oversized body, wrong content-type, concurrent writes, DB-down, unicode/injection payloads all unprobed â€” classic 500 triggers); (5) I1's 'no Stripe call' is inferred, not network-verified. Net: this proves 'no bypass' for the app's ownership/tier LOGIC (real production code) and honestly scopes itself, but does not prove it for the full auth stack, and provides no captured regression baseline.
- [low] Gate-semantic inconsistency: batch paywall returns 429 (E5) while book-cap and series paywalls return 403 (D2/D4/D5). HTTP 429 (Too Many Requests) wrongly signals rate/retry for what is a plan-feature gate, and is inconsistent with the sibling gates. Executor recorded E5 as a clean pass and did not flag the inconsistency.
- [low] I1 double-bill guard is claimed to short-circuit 'before any Stripe call / no Stripe network traffic', but the trace only evidences the 409 status/body. The no-Stripe-call property is inferred, not captured (no network log). The no-bypass 409 itself is genuinely proven; the network-absence claim is an over-statement.
- [low] All 34 probes authenticate via the x-e2e-test-secret + x-e2e-clerk-id test shim, so the production Clerk auth boundary (token forgery, expired/missing session, 401 paths) is never exercised. K4 proves only the shim's prefix guard, not production auth. 'Zero bypass' is demonstrated for the app's userId-scoping logic, not the full auth stack â€” a scoping caveat the D7 narrative under-frames.

### p1 / FUNCTIONALITY + RELIABILITY
SUSPICIOUSLY-CLEAN: No â€” this bundle is notably honest rather than suspiciously clean. It records failures raw and against interest: the framework HTML-404 on the discuss route at first contact (12_discuss-thread-probe.json), honest 400s, the 409 turn-cap, extraction throttled:true with a retry timestamp, and full disclosure of a concurrent co-driving agent and a coordinator HMR intervention. Its central positive claims all survived my independent byte-level re-verification (every anchorQuote/originalText in every anchored finding is a verbatim substring of the captured manuscript; the suppression tool_result is present verbatim in the SSE stream; memory content equals the turn-3 constraint byte-for-byte; chapter markdown is byte-identical before/mid/after; stats arithmetic is exact). What IS missing that I would expect: (1) zero screenshots/UI evidence â€” OBS-1's claim that the UI rescues the empty assistant bubble is unverifiable, and D3b/D4/D6 are blind spots; (2) the DiscussLLMEmptyErrorâ†’502 path never fired live (disclosed, but the D-04 fix's failure branch is still only unit-verified); (3) no export, autosave, or editor-save data-loss probes â€” D2's 'export never truncates / words never lost' promise is only indirectly evidenced via agent-side byte-stability; (4) no cross-user ownership or tier-gate negative probes in this bundle; (5) no clean-cold-boot rerun after the ENV-01 fix â€” the discuss journey only worked after a coordinator touched the route file, so 'would a fresh session complete the discuss journey unaided' remains unproven in this environment; (6) dev-edit #2's 'honored' claim rests on the model not re-attempting, not on a gate event. The two near-identical 30_findings-after-devedit*.json files from the co-driving agents contain identical finding IDs (checked), so the co-production did not manufacture inconsistency.
- [S3] Over-claim: defects.md states the honored-half of the FRESH discuss loop (memory bc68fab0/f1b35402) was 're-verified in dev-edit #2', but devedit2-rejudge-sse.jsonl contains NO suppression event for that lineage â€” the model simply never re-attempted the para-6 show-tell critique (only 1 CreateFinding, prose on para 2). This is absence-of-re-attempt evidence, the exact weaker evidence class the baseline D-13 caveat flagged; only the OLD lineage (cef57b13/25499afe) gate-fire in dev-edit #1 is live-triggered.
- [S3] Missed product defect: pending duplicate findings accumulate across dev-edit reruns â€” three PENDING show-tell findings (73b2781c 07-17, 5c20c0e1 07-18, 036a088d 07-20) all flag the same para-6 passage, and the writer's new memory constraint does not retro-dismiss pending siblings, so after Maya tells the editor to preserve the device she still faces 3 pending flags on exactly that passage (plus 2 anchor-less QA-seed junk findings inflating the daily-plan 'Review 6 editorial findings' count). Suppression covers only DISMISSED lineages; the 'tell it once' promise leaks through pending duplicates. Executor noted the mechanism but filed no defect.
- [S4] Evidence mis-citation: journey-log cites transcripts/discuss-f1b-turn1.json as 'the HTML-404 body' for ENV-01, but that file is a 400 {error:'Invalid JSON in request body'} captured at 11:38:34 â€” proving the route was ALREADY serving JSON before the claimed ~11:40 coordinator touch (and the 400 was caused by the capture attempt's own malformed body). The actual 404 HTML evidence is api-traces/12_discuss-thread-probe.json (11:27:54). ENV-01 itself stands; the citation and timeline are sloppy.
- [S4] Misquoted payload: journey-log Step 1 quotes the D-01 malformed body as '{"title": "Salt Letters Revised"  bad json here}}}' but the raw trace body is '{title: Salt'. Both are invalid JSON and the 400 verdict is unaffected, but the executor's quoted evidence does not match the trace.
- [S4] OBS-1's mitigation claim (constraint chip / AIRewriteComparison card surfaces the structured content when the bubble is empty) is asserted purely from source-code reading (finding-conversation.tsx) with zero screenshots in the bundle â€” unverifiable in-bundle. The raw verified fact remains: assistantMessage was the empty string on 2 of 3 discuss turns (turns 2 and 3), i.e. a residue of the D-04 symptom shape survives at the API level even though real substance ships in structured fields.

### p1 / UX / EXPERIENCE
SUSPICIOUSLY-CLEAN: No â€” this bundle is notably honest rather than suspiciously clean: it preserves the raw framework HTML-404 of the discuss route at first contact (12_discuss-thread-probe.json), the 409 turn-cap, the empty assistantMessage turns, honest 'not live-triggered' caveats for the 502 empty-reply path and D-33/34 rejection path, a concurrency disclosure for the co-driving agent's dev-edit, and a single-worker proof with full process lineage. My own re-derivations (sha256 of chapter content across 3 captures, byte-comparison of both finding anchors against the manuscript, stats arithmetic, daily-plan count at capture time, graph-entity grounding against the 704-word prose) all check out. HOWEVER, failure evidence I would expect and is missing: (1) ZERO screenshots or any UI capture â€” for a UX lens, D4 and D6 are unscoreable and the OBS-1 blank-bubble claim plus its claimed 'constraint chip' rescue are code-read assertions I cannot verify from the bundle; (2) the discuss feature only worked after a coordinator HMR-touched the route file â€” a real user on this exact environment would have hit a dead feature, and nothing shows how a production build behaves; (3) the DiscussLLMEmptyErrorâ†’502 path (the actual D-04 failure mode) never fired live, so the honest-failure behavior of discuss remains unproven; (4) no negative trust probes in this bundle (no cross-user ownership attempt, no tier-gate check, no export/truncation test); (5) part of the capture set was produced by a second co-driving agent and merged in â€” disclosed, but it means one bundle mixes two executors' evidence chains.
- [S3] Duplicate pending-finding accumulation not filed as a defect: dev-edit #1 created a 4th show-tell finding (036a088d) on the byte-identical anchorQuote already pending 3x (f1b35402/5c20c0e1/73b2781c share the same anchor text); executor framed it as 'legitimate NEW finding'. After f1b35402 was dismissed with a memory constraint, sibling duplicates 5c20c0e1/73b2781c/036a088d remained pending (32_findings-after-devedit2.json) â€” the writer must triage the same critique 3 more times; memory does not retro-resolve siblings. Real click-path/noise cost in the core findings UX.
- [S3] OBS-1 severity understated (S4 'cosmetic'): 2 of 3 assistant turns in the flagship discuss capture returned assistantMessage:"" (turns 2 and 3), meaning the majority of AI replies in the rejudged conversation render as blank bubbles per the executor's own code read (finding-conversation.tsx `??` doesn't catch empty string). Blank replies in the exact feature whose baseline defect was 'empty reply' is S3 friction, not cosmetic â€” and no screenshot proves the claimed constraint-chip/comparison rescue actually surfaces the content.
- [S4] Overclaim in executor narrative: journey-log/defects.md state the discuss loop returned 'real, non-empty, adaptive' / 'substantive, grounded, ADAPTIVE content every turn'. Literally false for the conversational channel â€” turns 2 and 3 have empty assistantMessage; substance exists only in structured fields. The D-04 reversal is real but the 'every turn non-empty' framing inflates it.
- [S4] Journey log quotes scan-1's 'throttled:false' but omits that the final continuity scan state reports throttled:true with retryEligibleAt ~1784548024509 (41_continuity-scan-final.json). The surfacing is honest (a positive), but the log's selective quotation hides the throttle state a user would see after extraction completes.
- [S4] Cross-persona test pollution on P1's book: two junk seeded findings ('Rita QA rate-limit target', 'Rita QA seeded finding', agentType 'agent', null anchors) sit pending in Maya's findings list and are counted in the daily plan's 'Review 6 editorial findings'. Not a product defect, but it contaminates the retention/daily-plan evidence and was not flagged by the executor.
- [S4] The 'honored' proof for the FRESH memory constraint rests on absence-of-re-flag only: dev-edit #2 (session 5574be0f) simply did not create a show-tell finding; unlike dev-edit #1's in-band suppression tool_result for the OLD memory row, there is no positive in-band evidence the new constraint (bc68fab0) gated anything â€” consistent with honor, but also with model stochasticity. Executor presents both halves as equally live-verified.

### p1 / TRUST + MANUSCRIPT INTELLIGENCE
SUSPICIOUSLY-CLEAN: No â€” this bundle is notably NOT suspiciously clean, and I byte-verified its core claims rather than trusting them. It contains genuine recorded failure evidence: the raw framework HTML-404 on the discuss route at first contact (12_discuss-thread-probe.json, ENV-01), an honest 409 turn-cap, honest extraction throttling (throttled:true surfaced in 41_continuity-scan-final.json), an explicit concurrency disclosure about a second agent's dev-edit, and honest caveats that the 502 empty-reply path and the CreateFinding rejection path were NOT live-triggered. All 10 finding anchors byte-match the manuscript, both chapter captures hash to the claimed sha256, and the SSE suppression event is present verbatim in the raw stream â€” the strongest claims survive re-derivation. What IS missing that I would expect: (1) any UI screenshot at all â€” the entire visual layer (including the OBS-1 blank-bubble claim) is argued from code citation, not shown; (2) any adversarial negative probe on ownership or tier gates (no attempt to read another user's book or exceed Indie-plan limits), so D7 rests solely on positive honesty evidence; (3) no export/download test for the manuscript (D2's 'export never truncates' promise untested); (4) the un-warmed undo and documents/versions routes were left 404 and untested; (5) the discuss loop was captured by a co-driving agent with the executor only independently GET-re-verifying the persisted thread â€” acceptable but one step removed for turn-level latency/streaming claims. The residual clean-ness is scope-narrowness, not sanitization.
- [S3] Duplicate pending findings accumulate: four near-identical show-tell findings on the exact same para-6 passage (73b2781c, 5c20c0e1, f1b35402, 036a088d) coexisted; the suppression gate only arms on DISMISSED lineage, so each dev-edit re-run adds another pending duplicate of an un-triaged critique. Writer's findings list is redundant clutter; executor framed 036a088d as 'legitimate NEW finding' and filed no defect.
- [S3] Dev Edit Report from session 5574be0f persists factual errors into a user-facing document: 'Chapter Word Count: ~570 words' (actual 704, byte-verified) and 'Edit Date: 2025' (actual 2026). Executor did not file this.
- [S3] Finding b92055ea flags 'has changed' as a tense clash inside the deliberately generic-present aphorism 'the way you let a burn sit before you touch it again, testing whether the pain has changed its mind'; its newText 'had changed' would break the generic-you present frame it sits in. Mild voice-flattening suggestion, persisted as pending (not caught/withheld) â€” triggers the D8 <= 8.0 cap. Executor did not assess it.
- [S3] Discuss turns 2 and 3 returned assistantMessage:"" with all substance in structured fields (revisedSuggestion / suggestedConstraint); per executor's own OBS-1 code citation the thread bubble renders blank for those turns. Executor filed it as S4 cosmetic, but 2 of 3 live turns in P1's flagship floor-driver feature rendering an empty bubble is a state-visibility defect, S3 â€” it is the visual ghost of the old D-04 symptom even though real content now exists.
- [S4] Journey log mis-cites the evidence for the mother's deathChapter inference â€” it quotes 'the war took the shape out of him... loved her mother', a line about Thomas Rhys; the actual manuscript support is the funeral/grief passage in para 2. The inference itself is correct (mother is dead), but the cited quote does not support it. Evidence-hygiene error, and the deathChapter:1 attribution for a pre-narrative backstory death is a modeling convention that could seed future flashback false positives.
- [S4] Daily plan 'Review 6 editorial findings' count includes two null-anchor junk findings seeded by another persona's rate-limit test ('Rita QA', agentType 'agent', no anchorQuote/suggestion) â€” cross-persona test contamination inflating an actionable count shown to the writer. Mostly a QA-environment artifact, but the endpoint counts non-actionable rows.
- [S4] Evidence-scope gaps, not misrepresentations: zero UI screenshots (D3b/D4/D6 unevidenced), no negative ownership/cross-user probe and no tier-gate test for the Indie plan (limits D7 to positive-honesty evidence), no export test, and the discuss 502 empty-reply path plus D-33/34 rejection paths remain live-untriggered (executor disclosed the latter two honestly).

### p7 / FUNCTIONALITY + RELIABILITY
SUSPICIOUSLY-CLEAN: No â€” this bundle does not look suspiciously clean, and I attempted to break its claims directly. I independently re-derived every headline claim from raw bytes and they all held: 80/80 sentinels in order in export.docx AND export.epub AND export-reordered.docx (my own unzip, not their analysis JSON); dc:title/OPF-title//Info-Title all full-diacritic and matching book.name; PDF page count 100 confirmed two ways (my raw scan's 200 matches decomposed into exactly 100 /Type /Page + 100 /Type /PageLabel, plus /Count 100); reversal order confirmed positionally; zero 500s and no masked statuses in any trace (409s and 404s recorded raw = honest failures, the POSITIVE kind). Crucially, the bundle self-reports real negatives rather than hiding them: D-46 residual (Ã†rÃ¸â†’'r' visible in the actual returned filenames), D-57 PARTIAL with a live inconsistent 404, EPUB front-matter title residue, and an explicit NOT-TESTABLE declaration that PDF prose is glyph-encoded and could not be literally extracted â€” an honest hole in the truncation net rather than a claimed pass. Failure evidence I would expect but is missing: (1) an interrupted/killed-mid-save probe (the persona's core nightmare); (2) the markdown export leg named in the persona focus; (3) any cross-user/tier-gate adversarial probe; (4) any UI capture at all (pure API bundle â€” D3b/D4/D6/D8/D9 are unscoreable); (5) stress above 51K words; (6) the executor recorded a mojibake XMP string their own analyzer produced and never chased it â€” I did, and found a real (tiny) product inconsistency ('---' vs em dash in XMP). These are coverage gaps, not concealment: the shape of the evidence (raw statuses, self-reported residuals, declared limitations) is consistent with an honest capture, and D2's strength (9.0) rests on my own byte-level confirmation, with the named caveat that PDF prose integrity is only indirectly assured.
- [S4 (product) + evidence-hygiene miss] PDF XMP dc:title carries literal '---' where /Info /Title has the em dash 'â€”' (typst smart-dash source leaked into XMP); Acrobat-class readers preferring XMP will show a subtly wrong title. Compounding: the executor's recorded xmp value in pdf-analysis.json is mojibake from their own analyzer (UTF-8 bytes decoded as latin-1) and was recorded but never investigated or flagged â€” I resolved it from the raw PDF bytes myself.
- [low (coverage gap)] Markdown export leg never exercised despite the persona focus explicitly naming export (docx/pdf/md); bundle covers docx/pdf/epub only, so md truncation/metadata integrity is unverified.
- [low (coverage gap)] No interrupted-write / kill-mid-PUT / crash-recovery probe â€” the canonical 'never lose a word' failure scenario for this persona (connection dies mid-save) is untested; all reliability evidence is from completed requests.
- [low (coverage gap)] No cross-user ownership probe in this bundle (every request authenticated as user_qa_p7); post-delete 404s evidence clean fencing for the owner only, so D7 rests partly on prior-cycle evidence. Also the '9-route Not-found residual' figure for D-57 is source-grep-derived, not live-derived â€” only 1 of the 9 (GET /continuity) is confirmed by live trace in this bundle.
- [info] Scale ceiling is 51K words / 16 chapters with a max single-chapter size of ~9K chars during autosave; no single-huge-chapter (50K-word) or 200K-word-book stress despite the 'at-scale' framing. 51K is novel-length and reasonable, but 'at scale' is only partially earned.

### p7 / UX / EXPERIENCE
SUSPICIOUSLY-CLEAN: Partially. Every product-side probe in this bundle PASSes (zero 500s, zero data loss, all drills green), which pattern-matches 'suspiciously clean' â€” but three things counter it: (1) the probes are genuinely adversarial (stampless concurrency stampede, full reorder+re-export with sentinel pairing, 17-probe delete drill, multi-script Unicode fixture designed to break filename/metadata paths) and they DID surface residuals (Aero->'r' filename mangling, 9-route 'Not found' copy inconsistency, EPUB front-matter head-title residue), so it is not scrubbed; (2) the executor honestly declares NOT-TESTABLE limits (PDF glyph-encoded prose) rather than over-claiming; (3) I independently re-derived the headline claims from the raw binaries and they held, and I caught the executor UNDER-claiming in one place (XMP mojibake was their decode artifact, though they missed the real '---' em-dash residual). Failure evidence I would still expect but is missing: no markdown-fallback export test, no oversized/pathological input (500K-word book, single multi-MB chapter â€” 51K words is modest for 'writes long'), no process-kill mid-autosave durability test, no cross-user ownership 403/404 probe, and zero UI screenshots (state visibility, save indicators, conflict-resolution UX are all unverified at the interface where Bao would actually experience them). The scope also conspicuously tracks the just-landed fix list (D-05/46/47/57/61), i.e., it re-tests known-fixed paths more than it hunts new floors.
- [minor] Evidence misrepresentation (minor): pdf-analysis.json 'xmp' field shows mojibake ('KÃ…szegâ€¦ÃƒrÃƒÂ¸') which is the executor's latin-1 decode of UTF-8 bytes, not the PDF's real state; real XMP decodes to correct diacritics. Executor presented it unresolved, making the PDF look worse than it is.
- [S4-cosmetic] Missed real residual: PDF XMP dc:title contains '---' where the book name has an em-dash 'â€”' (typst smart-punctuation leaking into XMP); readers that prefer XMP over /Info (e.g. Acrobat) will show the wrong title punctuation. Not filed as a finding.
- [minor] Coverage gap: markdown export (the pandoc-fallback format, explicitly in the persona brief 'docx/pdf/md') was never exercised; only docx/pdf/epub tested.
- [info] Unreconciled metric drift: export API wordCount 51,436 vs book wordCount 51,291 (+145, presumably titles/front-matter) never explained â€” for a data-safety persona every word-count delta should be accounted for.
- [info] Determinism claim not independently checkable: 'body XML byte-identical across re-exports' rests on the in-script comparison; only one reordered docx (export-reordered.docx) shipped in the bundle, so a judge cannot re-derive the byte-identity claim.
- [info] Coverage gap: no mid-save interruption/crash-recovery drill and 'at scale' capped at 51K words / 20K-char chapters â€” modest for a persona defined by 'writes long, never loses a word'; the untested stress negatives are where the next floor could hide.

### p7 / TRUST + MANUSCRIPT INTELLIGENCE
SUSPICIOUSLY-CLEAN: Moderately clean, but not suspiciously so â€” and crucially not fabricated: I independently re-derived every headline claim from the raw artifacts (unzipped both docx files and the epub myself: 80/80 sentinels in correct/reversed order, dc:title byte-equal in docx+epub; parsed the PDF myself: 100 page objects + /Count 100, /Info UTF-16BE title byte-equal, glyph-encoded text confirming the honest NOT-TESTABLE disclosure), and the traces are internally consistent (monotonic timestamps, consistent UUIDs across all 8 traces). The executor also filed honest negatives against their own pass (D-46 residual 'Ã†rÃ¸'â†’'r', D-57 residual live-demonstrated, EPUB front-matter title residue, PDF text not directly verifiable). What IS missing, and what I would expect from a true bulletproof pass for this persona: (1) any 4xx/5xx on the export pipeline â€” every export request is happy-path, so export's failure honesty (invalid format, empty book, renderer missing, markdown fallback, interrupted download) is completely unexercised; (2) any browser/UI capture at all â€” zero screenshots; the editor's real autosave loop, conflict UX for the 409s, and offline/tab-close behavior are unproven, which matters because the persona promise is client-level 'never lose a word'; (3) cross-tenant ownership and tier-gate probes; (4) a genuinely huge single chapter (the 51K words are spread across 16 x ~3.2K-word chapters); (5) storage/disk failure injection during export. The cleanliness reads as deliberate scope limitation honestly labeled, not evidence laundering.
- [low] PDF XMP dc:title degrades the em dash to '---' (XMP reads 'The KÅ‘szeg Manuscript --- Bao RÃ©judge P7 ...' while /Info carries the correct 'â€”'). Readers that prefer XMP over /Info (e.g. Adobe) will show a subtly wrong title. Executor captured the xmp field in pdf-analysis.json but never flagged the mismatch.
- [low] pdf-analysis.json 'xmp' field is presented as mojibake ('The KÃ…szeg...') because the analysis script decoded valid UTF-8 XMP bytes as latin-1; I verified raw bytes 4b c5 91 = 'KÅ‘' are correct. Misleading evidence presentation â€” a bundle reader could wrongly conclude the PDF XMP is double-encoded.
- [medium] 'Autosave-at-scale' overstates its coverage: it is 15 sequential server-side API PUTs on a chapter that ends at ~1,369 words. No browser-level autosave loop, no tab-close/offline case, no very large single chapter (persona 'writes long'). The server versioning contract is proven; the client never-lose-a-word path is not.
- [medium] Export path has zero negative/failure-injection tests â€” all export calls are happy-path 200s. Invalid format, empty book, pandoc/typst-missing markdown fallback, and interrupted download are unexercised, so the honest-failure side of the export pipeline is unproven in this bundle.
- [low] D-57 residual claim of '9 routes returning bare Not found' rests on a static source grep whose output is not in the bundle; only 1 of 9 (GET /continuity) is live-verified. Slight over-claim beyond captured evidence (plausible but unproven for the other 8).
- [low] Export API wordCount (51,436) differs from book wordCount (51,291) by +145 with no explanation surfaced or flagged (likely chapter titles/front-matter counted by the export pipeline). Harmless but an unexplained number inconsistency a data-safety persona would question.
- [medium] No cross-tenant ownership probes anywhere in the bundle (e.g. another user's clerk-id against Bao's book/exports), so D7 fencing is only indirectly evidenced via post-delete 404s; tier/plan gating on export also untested.

